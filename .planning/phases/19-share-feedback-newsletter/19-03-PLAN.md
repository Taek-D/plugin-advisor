---
phase: 19-share-feedback-newsletter
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - components/FeedbackWidget.tsx
  - components/NewsletterForm.tsx
  - app/api/feedback/route.ts
  - app/api/newsletter/route.ts
  - app/admin/feedback/page.tsx
  - app/layout.tsx
  - app/page.tsx
  - app/admin/suggestions/page.tsx
  - lib/__tests__/feedback-route.test.ts
  - lib/__tests__/newsletter-route.test.ts
autonomous: true
requirements:
  - FDBK-01
  - FDBK-02
  - FDBK-03
  - NEWS-01
  - NEWS-02

must_haves:
  truths:
    - "사이트 모든 페이지에서 우하단 플로팅 피드백 버튼이 보인다"
    - "피드백 버튼 클릭 시 슬라이드업 드로어가 나타나고 타입 선택 + 메시지 입력 후 제출하면 Supabase에 저장된다"
    - "피드백 API가 rate limit을 적용한다 (시간당 5회)"
    - "관리자가 /admin/feedback에서 피드백 목록을 확인할 수 있다"
    - "랜딩 페이지 푸터 위에 뉴스레터 구독 폼이 표시된다"
    - "이메일 구독 시 Supabase에 저장되고, 중복 이메일은 성공처럼 보여진다 (upsert)"
    - "뉴스레터 API가 rate limit을 적용한다 (시간당 3회)"
  artifacts:
    - path: "components/FeedbackWidget.tsx"
      provides: "플로팅 피드백 버튼 + 슬라이드업 드로어"
      exports: ["default"]
      min_lines: 60
    - path: "components/NewsletterForm.tsx"
      provides: "이메일 구독 폼 컴포넌트"
      exports: ["default"]
      min_lines: 40
    - path: "app/api/feedback/route.ts"
      provides: "피드백 POST API (rate limit + Supabase insert)"
      exports: ["POST"]
    - path: "app/api/newsletter/route.ts"
      provides: "뉴스레터 POST API (rate limit + Supabase upsert)"
      exports: ["POST"]
    - path: "app/admin/feedback/page.tsx"
      provides: "관리자 피드백 목록 페이지"
      exports: ["default"]
    - path: "lib/__tests__/feedback-route.test.ts"
      provides: "피드백 API 유닛 테스트"
      min_lines: 40
    - path: "lib/__tests__/newsletter-route.test.ts"
      provides: "뉴스레터 API 유닛 테스트"
      min_lines: 40
  key_links:
    - from: "components/FeedbackWidget.tsx"
      to: "app/api/feedback/route.ts"
      via: "fetch POST /api/feedback"
      pattern: "fetch.*api/feedback"
    - from: "components/NewsletterForm.tsx"
      to: "app/api/newsletter/route.ts"
      via: "fetch POST /api/newsletter"
      pattern: "fetch.*api/newsletter"
    - from: "app/api/feedback/route.ts"
      to: "lib/supabase-admin.ts"
      via: "getSupabaseAdminClient().from('feedback').insert()"
      pattern: "from.*feedback.*insert"
    - from: "app/api/newsletter/route.ts"
      to: "lib/supabase-admin.ts"
      via: "getSupabaseAdminClient().from('newsletter_subscribers').upsert()"
      pattern: "from.*newsletter.*upsert"
    - from: "app/layout.tsx"
      to: "components/FeedbackWidget.tsx"
      via: "JSX render inside I18nProvider"
      pattern: "<FeedbackWidget"
    - from: "app/page.tsx"
      to: "components/NewsletterForm.tsx"
      via: "JSX render above footer section"
      pattern: "<NewsletterForm"
---

<objective>
FeedbackWidget(플로팅 피드백), NewsletterForm(뉴스레터 구독), 그리고 이들의 백엔드 API(/api/feedback, /api/newsletter)와 관리자 피드백 확인 페이지를 구현하고, 기존 페이지에 통합한다.

Purpose: 사용자 의견 수집으로 제품 개선 피드백 루프를 만들고, 뉴스레터로 재방문 채널을 확보한다.
Output: FeedbackWidget + NewsletterForm + API 2개 + 관리자 페이지 + 테스트 + 페이지 통합
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-share-feedback-newsletter/19-RESEARCH.md
@.planning/phases/19-share-feedback-newsletter/19-01-SUMMARY.md

@app/layout.tsx
@app/page.tsx
@app/api/plugin-suggestions/route.ts
@app/admin/suggestions/page.tsx
@lib/rate-limit.ts
@lib/supabase-admin.ts
@lib/admin-session.ts
@lib/i18n/types.ts

<interfaces>
<!-- From Plan 19-01 outputs -->

From lib/i18n/types.ts (extended in 19-01):
```typescript
feedback: {
  buttonLabel: string;
  panelTitle: string;
  typeBug: string;
  typeFeature: string;
  typeOther: string;
  messagePlaceholder: string;
  submit: string;
  submitting: string;
  successMsg: string;
  errorMsg: string;
  close: string;
};
newsletter: {
  sectionTitle: string;
  sectionDesc: string;
  placeholder: string;
  submit: string;
  submitting: string;
  successMsg: string;
  errorMsg: string;
  privacyNote: string;
};
```

From lib/analytics.ts (extended in 19-01):
```typescript
export function trackEvent(event: EventName, payload?: EventPayload): void;
// EventName includes "feedback_open", "feedback_submit", "newsletter_subscribe", "newsletter_error"
```

From app/api/plugin-suggestions/route.ts (exact template to follow):
```typescript
import { NextRequest, NextResponse } from "next/server";
import { checkRateLimit, cleanupExpiredEntries } from "@/lib/rate-limit";
import { SupabaseNotConfiguredError } from "@/lib/supabase-admin";

export async function POST(request: NextRequest) {
  try {
    cleanupExpiredEntries();
    const { allowed } = checkRateLimit(request, { name: "...", maxRequests: 3, windowMs: 60 * 60_000 });
    if (!allowed) { return NextResponse.json({ error: "..." }, { status: 429 }); }
    // ... validate body, insert to Supabase ...
    return NextResponse.json({ ok: true }, { status: 201 });
  } catch (error) {
    if (error instanceof SupabaseNotConfiguredError) { return NextResponse.json({ error: "..." }, { status: 503 }); }
    return NextResponse.json({ error: "..." }, { status: 500 });
  }
}
```

From app/admin/suggestions/page.tsx (exact admin page template):
```typescript
import { requireAdminSession } from "@/lib/admin-session";
import { getSupabaseAdminClient } from "@/lib/supabase-admin";
export const dynamic = "force-dynamic";
export default async function AdminSuggestionsPage({ searchParams }: Props) {
  requireAdminSession("/admin/suggestions");
  // ... fetch data, render list ...
}
```

From app/layout.tsx (FeedbackWidget integration point):
```typescript
// FeedbackWidget goes BEFORE closing </I18nProvider> tag, AFTER footer
<I18nProvider>
  <Nav />
  <div className="min-h-[calc(100vh-3.5rem)]">{children}</div>
  <footer>...</footer>
  {/* <FeedbackWidget /> goes HERE */}
</I18nProvider>
```

From lib/rate-limit.ts:
```typescript
export function checkRateLimit(request: NextRequest, options: { name: string; maxRequests: number; windowMs: number }): { allowed: boolean; remaining: number; resetAt: number };
export function cleanupExpiredEntries(): void;
```

From lib/supabase-admin.ts:
```typescript
export class SupabaseNotConfiguredError extends Error;
export function getSupabaseAdminClient(): SupabaseClient;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /api/feedback + /api/newsletter API 라우트 + 유닛 테스트</name>
  <files>app/api/feedback/route.ts, app/api/newsletter/route.ts, lib/__tests__/feedback-route.test.ts, lib/__tests__/newsletter-route.test.ts</files>
  <behavior>
    - POST /api/feedback: type(bug|feature|other) + message(1~500자) + page를 받아 Supabase feedback 테이블에 insert, rate limit 5/hour
    - POST /api/feedback: message가 비거나 500자 초과 시 400 반환
    - POST /api/feedback: rate limit 초과 시 429 반환
    - POST /api/feedback: Supabase 미설정 시 503 반환
    - POST /api/newsletter: email을 받아 Supabase newsletter_subscribers에 upsert (ignoreDuplicates), rate limit 3/hour
    - POST /api/newsletter: 이메일 형식 오류 시 400 반환
    - POST /api/newsletter: 중복 이메일이어도 201 반환 (upsert 효과)
    - POST /api/newsletter: rate limit 초과 시 429 반환
  </behavior>
  <action>
**테스트 먼저 작성 (RED):**

**lib/__tests__/feedback-route.test.ts:**
기존 `lib/__tests__/plugin-suggestions.test.ts` 패턴을 따름. `vi.mock("next/server")` 패턴으로 NextRequest/NextResponse를 모킹 (프로젝트에서 확립된 패턴 — STATE.md 결정 참조).

테스트 케이스:
1. 유효한 body로 POST → 201 + Supabase insert 호출 확인
2. message 비어있으면 → 400
3. type이 유효하지 않으면 → 400
4. rate limit 초과 → 429
5. Supabase 미설정 → 503

Supabase 클라이언트는 `vi.mock("@/lib/supabase-admin")`으로 모킹. rate-limit은 `vi.mock("@/lib/rate-limit")`으로 모킹.

**lib/__tests__/newsletter-route.test.ts:**
테스트 케이스:
1. 유효한 email로 POST → 201 + Supabase upsert 호출 확인
2. email이 비어있거나 형식 오류 → 400
3. 중복 email (upsert이므로 에러 없음) → 201
4. rate limit 초과 → 429
5. Supabase 미설정 → 503

**이후 구현 (GREEN):**

**app/api/feedback/route.ts:**
```typescript
import { NextRequest, NextResponse } from "next/server";
import { checkRateLimit, cleanupExpiredEntries } from "@/lib/rate-limit";
import { getSupabaseAdminClient, SupabaseNotConfiguredError } from "@/lib/supabase-admin";

const VALID_TYPES = ["bug", "feature", "other"] as const;
type FeedbackType = (typeof VALID_TYPES)[number];

export async function POST(request: NextRequest) {
  try {
    cleanupExpiredEntries();
    const { allowed } = checkRateLimit(request, {
      name: "feedback",
      maxRequests: 5,
      windowMs: 60 * 60_000,
    });
    if (!allowed) {
      return NextResponse.json(
        { error: "피드백 제출이 너무 많습니다. 1시간 후 다시 시도해 주세요." },
        { status: 429 }
      );
    }

    const body = await request.json();
    const message = typeof body.message === "string" ? body.message.trim() : "";
    const type = typeof body.type === "string" && VALID_TYPES.includes(body.type as FeedbackType)
      ? (body.type as FeedbackType)
      : null;
    const page = typeof body.page === "string" ? body.page : "unknown";

    if (!message || message.length > 500) {
      return NextResponse.json(
        { error: "메시지는 1~500자여야 합니다." },
        { status: 400 }
      );
    }
    if (!type) {
      return NextResponse.json(
        { error: "유효한 피드백 유형을 선택해 주세요." },
        { status: 400 }
      );
    }

    const supabase = getSupabaseAdminClient();
    const { error } = await supabase.from("feedback").insert({
      page,
      message,
      type,
    });
    if (error) throw error;

    return NextResponse.json({ ok: true }, { status: 201 });
  } catch (error) {
    if (error instanceof SupabaseNotConfiguredError) {
      return NextResponse.json(
        { error: "피드백 기능이 현재 사용할 수 없습니다." },
        { status: 503 }
      );
    }
    return NextResponse.json(
      { error: "피드백 저장 중 오류가 발생했습니다." },
      { status: 500 }
    );
  }
}
```

**app/api/newsletter/route.ts:**
```typescript
import { NextRequest, NextResponse } from "next/server";
import { checkRateLimit, cleanupExpiredEntries } from "@/lib/rate-limit";
import { getSupabaseAdminClient, SupabaseNotConfiguredError } from "@/lib/supabase-admin";

const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export async function POST(request: NextRequest) {
  try {
    cleanupExpiredEntries();
    const { allowed } = checkRateLimit(request, {
      name: "newsletter",
      maxRequests: 3,
      windowMs: 60 * 60_000,
    });
    if (!allowed) {
      return NextResponse.json(
        { error: "구독 시도가 너무 많습니다. 1시간 후 다시 시도해 주세요." },
        { status: 429 }
      );
    }

    const body = await request.json();
    const email = typeof body.email === "string" ? body.email.trim().toLowerCase() : "";

    if (!email || !EMAIL_RE.test(email)) {
      return NextResponse.json(
        { error: "유효한 이메일 주소를 입력해 주세요." },
        { status: 400 }
      );
    }

    const supabase = getSupabaseAdminClient();
    const { error } = await supabase
      .from("newsletter_subscribers")
      .upsert(
        { email, source: "landing", confirmed: false },
        { onConflict: "email", ignoreDuplicates: true }
      );
    if (error) throw error;

    // 중복이든 신규든 항상 성공 응답 — 이메일 존재 여부 비노출 (보안)
    return NextResponse.json({ ok: true }, { status: 201 });
  } catch (error) {
    if (error instanceof SupabaseNotConfiguredError) {
      return NextResponse.json(
        { error: "뉴스레터 구독 기능이 현재 사용할 수 없습니다." },
        { status: 503 }
      );
    }
    return NextResponse.json(
      { error: "구독 처리 중 오류가 발생했습니다." },
      { status: 500 }
    );
  }
}
```

**주의사항:**
- newsletter는 `.insert()`가 아닌 `.upsert({ onConflict: "email", ignoreDuplicates: true })` 사용 — unique constraint 에러 방지
- feedback의 rating 컬럼은 null로 두기 (Phase 17에서 생성됐지만 locked decision에서 별점 UI 제거됨)
- 에러 메시지는 한국어로 (한국어 UI 기본 원칙)
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm test -- feedback-route && pnpm test -- newsletter-route</automated>
  </verify>
  <done>두 API 라우트가 rate limit + Supabase 연동으로 동작하고, 각각의 유닛 테스트가 모든 경로(성공/400/429/503)를 검증함.</done>
</task>

<task type="auto">
  <name>Task 2: FeedbackWidget + NewsletterForm 컴포넌트 + 관리자 페이지 + 페이지 통합</name>
  <files>components/FeedbackWidget.tsx, components/NewsletterForm.tsx, app/admin/feedback/page.tsx, app/layout.tsx, app/page.tsx, app/admin/suggestions/page.tsx</files>
  <action>
**1. components/FeedbackWidget.tsx** — 'use client' 컴포넌트:

플로팅 버튼 (fixed, bottom-4 right-4, z-50) + 슬라이드업 드로어 (CSS translate-y transition).

구현:
- `useState`로 `open`, `type`("bug"|"feature"|"other"), `message`, `submitting`, `submitted` 관리
- 플로팅 버튼: `MessageSquare` 아이콘 (lucide-react) + `t.feedback.buttonLabel` — 원형 또는 pill 형태, `bg-primary text-primary-foreground`
- 드로어 패널: `fixed bottom-16 right-4 sm:right-6 z-50 w-80` — open 시 `translate-y-0 opacity-100`, closed 시 `translate-y-4 opacity-0 pointer-events-none`, `transition-all duration-300`
- 타입 선택: 3개 라디오 버튼 (bug/feature/other) — `t.feedback.typeBug/typeFeature/typeOther`
- 메시지: `<Textarea>` (shadcn) — `t.feedback.messagePlaceholder`, maxLength 500
- 제출 버튼: `t.feedback.submit` / `t.feedback.submitting`
- 성공 시: `t.feedback.successMsg` 표시 → 1.5초 후 자동 닫기 + 폼 리셋
- 에러 시: `t.feedback.errorMsg` 표시
- 닫기: X 버튼 (`t.feedback.close`) 또는 성공 후 자동
- submit handler: `fetch("/api/feedback", { method: "POST", body: JSON.stringify({ type, message, page: window.location.pathname }) })`
- analytics: open 시 `trackEvent("feedback_open")`, submit 성공 시 `trackEvent("feedback_submit", { type })`

**200줄 제한 주의:** 컴포넌트를 간결하게 유지. inline state 관리, 별도 서브 컴포넌트 분리 불필요 (단일 목적 위젯).

**2. components/NewsletterForm.tsx** — 'use client' 컴포넌트:

인라인 이메일 구독 폼 (랜딩 페이지 푸터 위 섹션).

구현:
- `useState`로 `email`, `submitting`, `status`("idle"|"success"|"error") 관리
- 섹션 타이틀: `t.newsletter.sectionTitle`
- 설명: `t.newsletter.sectionDesc`
- 이메일 입력: `<Input>` (shadcn) — type="email", `t.newsletter.placeholder`
- 제출 버튼: `t.newsletter.submit` / `t.newsletter.submitting`
- 성공: `t.newsletter.successMsg` — 인라인 텍스트로 폼 대체
- 에러: `t.newsletter.errorMsg` — 인라인 에러 메시지
- 하단: `t.newsletter.privacyNote` — 작은 텍스트
- submit handler: `fetch("/api/newsletter", { method: "POST", body: JSON.stringify({ email }) })`
- analytics: 성공 시 `trackEvent("newsletter_subscribe")`, 에러 시 `trackEvent("newsletter_error")`

**레이아웃:** 가운데 정렬, max-w-md, 이메일 입력 + 버튼을 한 줄에 (flex row), 모바일에서 스택.

**3. app/admin/feedback/page.tsx** — 서버 컴포넌트:

`app/admin/suggestions/page.tsx` 패턴을 정확히 따름:
- `export const dynamic = "force-dynamic";`
- `requireAdminSession("/admin/feedback");`
- `getSupabaseAdminClient().from("feedback").select("*").order("created_at", { ascending: false })`
- 각 피드백 항목: type 배지 (bug=red, feature=blue, other=gray) + message + page + created_at
- 상단 nav에 "플러그인 관리", "제안 검토" 링크 (기존 admin 페이지 패턴)
- `AdminLogoutButton` 포함

**4. app/layout.tsx 통합 (FeedbackWidget):**
- `import FeedbackWidget from "@/components/FeedbackWidget";` 추가
- `</I18nProvider>` 닫기 태그 바로 전, `</footer>` 바로 뒤에 `<FeedbackWidget />` 삽입:
```tsx
<I18nProvider>
  <Nav />
  <div className="min-h-[calc(100vh-3.5rem)]">{children}</div>
  <footer>...</footer>
  <FeedbackWidget />
</I18nProvider>
```

**5. app/page.tsx 통합 (NewsletterForm):**
- `import NewsletterForm from "@/components/NewsletterForm";` 추가
- 랜딩 페이지 `</main>` 닫기 태그 바로 전에 뉴스레터 섹션 삽입. 기존 마지막 섹션과 `</main>` 사이에 배치:
```tsx
{/* Newsletter Section */}
<section className="mx-auto max-w-6xl px-4 py-16 sm:px-6">
  <NewsletterForm />
</section>
</main>
```

**6. app/admin/suggestions/page.tsx — 피드백 링크 추가:**
- 기존 "플러그인 관리" 링크 옆에 "피드백 확인" 링크 추가:
```tsx
<Link href="/admin/feedback" className="inline-flex h-10 items-center rounded-full border border-input px-4 text-sm font-medium text-muted-foreground transition-colors hover:bg-accent hover:text-foreground">
  피드백 확인
</Link>
```
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>FeedbackWidget이 layout.tsx에서 전역 렌더링되고, NewsletterForm이 랜딩 페이지 푸터 위에 렌더링되고, /admin/feedback 페이지가 동작하고, suggestions 페이지에 피드백 링크가 추가됨. typecheck + lint 통과.</done>
</task>

</tasks>

<verification>
pnpm typecheck && pnpm test -- feedback-route && pnpm test -- newsletter-route && pnpm lint && pnpm build
</verification>

<success_criteria>
- FeedbackWidget이 사이트 전체에서 우하단 플로팅 버튼으로 표시되고, 드로어에서 type + message 제출 가능
- /api/feedback POST가 rate limit(5/hour) + Supabase insert로 동작, 유닛 테스트 통과
- /admin/feedback에서 피드백 목록 확인 가능 (관리자 인증 필요)
- NewsletterForm이 랜딩 페이지 푸터 위에 표시되고 이메일 구독 가능
- /api/newsletter POST가 rate limit(3/hour) + Supabase upsert로 동작, 중복 이메일도 201 반환, 유닛 테스트 통과
- pnpm typecheck + lint + build 모두 통과
</success_criteria>

<output>
After completion, create `.planning/phases/19-share-feedback-newsletter/19-03-SUMMARY.md`
</output>
