---
phase: 19-share-feedback-newsletter
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/i18n/types.ts
  - lib/i18n/ko.ts
  - lib/i18n/en.ts
  - components/ShareResultButton.tsx
  - app/guides/page.tsx
  - app/guides/[slug]/GuideDetailClient.tsx
autonomous: true
gap_closure: true
requirements:
  - SHAR-01
  - SHAR-03
  - NEWS-01

must_haves:
  truths:
    - "데스크톱에서 X와 LinkedIn 직접 공유 링크가 ShareResultButton에 표시된다"
    - "/guides 목록 페이지에서 뉴스레터 구독 폼이 하단에 표시된다"
    - "/guides/[slug] 상세 페이지에서 뉴스레터 구독 폼이 하단에 표시된다"
  artifacts:
    - path: "components/ShareResultButton.tsx"
      provides: "X/LinkedIn 소셜 공유 링크 포함"
      contains: "twitter.com/intent/tweet"
    - path: "app/guides/page.tsx"
      provides: "NewsletterForm 렌더링"
      contains: "NewsletterForm"
    - path: "app/guides/[slug]/GuideDetailClient.tsx"
      provides: "NewsletterForm 렌더링"
      contains: "NewsletterForm"
  key_links:
    - from: "components/ShareResultButton.tsx"
      to: "twitter.com/intent/tweet"
      via: "window.open or anchor href"
      pattern: "twitter\\.com/intent/tweet"
    - from: "components/ShareResultButton.tsx"
      to: "linkedin.com/sharing"
      via: "window.open or anchor href"
      pattern: "linkedin\\.com/sharing"
    - from: "app/guides/page.tsx"
      to: "components/NewsletterForm.tsx"
      via: "JSX import and render"
      pattern: "NewsletterForm"
---

<objective>
Close 2 verification gaps from 19-VERIFICATION.md: (1) Add X/Twitter and LinkedIn direct share links to ShareResultButton for desktop fallback, and (2) integrate NewsletterForm into /guides and /guides/[slug] pages.

Purpose: ROADMAP SC#1 requires "X, LinkedIn으로 공유" and SC#3 requires "소셜 링크 폴백". SC#5 requires "랜딩 페이지와 /guides에서 이메일 구독 폼". Currently only clipboard copy exists on desktop and NewsletterForm only exists on the landing page.

Output: Updated ShareResultButton with social share links, NewsletterForm on both guide pages, extended i18n keys.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-share-feedback-newsletter/19-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/i18n/types.ts (lines 217-221 — current share section):
```typescript
share: {
  button: string;
  copied: string;
  title: string;
};
```

From lib/i18n/ko.ts (lines 216-220):
```typescript
share: {
  button: "공유하기",
  copied: "복사됨",
  title: "Plugin Advisor 추천 결과",
},
```

From lib/i18n/en.ts (lines 217-221):
```typescript
share: {
  button: "Share",
  copied: "Copied",
  title: "Plugin Advisor Results",
},
```

From components/ShareResultButton.tsx (full file, 38 lines):
- Uses `shareResult()` from lib/share-utils.ts
- Shows Copy→Check state transition for clipboard path
- Gets URL from `window.location.href` inside click handler

From components/NewsletterForm.tsx:
- Self-contained 'use client' component, 83 lines
- Uses `useI18n()` for t.newsletter.* keys
- No props required — can be dropped into any page

From app/guides/page.tsx:
- 'use client' component, uses `useI18n()` for locale
- Renders guide cards in a grid

From app/guides/[slug]/GuideDetailClient.tsx:
- 'use client' component receiving `guide: StarterGuide` prop
- Renders guide detail with sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add social share i18n keys + X/LinkedIn links to ShareResultButton</name>
  <files>lib/i18n/types.ts, lib/i18n/ko.ts, lib/i18n/en.ts, components/ShareResultButton.tsx</files>
  <action>
1. Extend the `share` section in `lib/i18n/types.ts` by adding two new keys:
   ```typescript
   share: {
     button: string;
     copied: string;
     title: string;
     shareOnX: string;      // NEW
     shareOnLinkedin: string; // NEW
   };
   ```

2. Add corresponding values in `lib/i18n/ko.ts`:
   ```typescript
   shareOnX: "X에 공유",
   shareOnLinkedin: "LinkedIn에 공유",
   ```

3. Add corresponding values in `lib/i18n/en.ts`:
   ```typescript
   shareOnX: "Share on X",
   shareOnLinkedin: "Share on LinkedIn",
   ```

4. Update `components/ShareResultButton.tsx` to add social share links below the existing copy/share button. The component should:
   - Keep the existing shareResult() + Copy→Check button as the primary action
   - Add two small icon-link buttons below (or beside) the main button for X and LinkedIn
   - X share URL: `https://twitter.com/intent/tweet?url=${encodeURIComponent(window.location.href)}&text=${encodeURIComponent(t.share.title)}`
   - LinkedIn share URL: `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(window.location.href)}`
   - Open in new tab: `window.open(url, '_blank', 'noopener,noreferrer')`
   - Track events: `trackEvent("result_share", { outcome: "x" })` and `trackEvent("result_share", { outcome: "linkedin" })`
   - Use simple text buttons with `variant="ghost" size="sm"` styling, no new icons needed (use text "X" and "in" or import Twitter/Linkedin from lucide-react if available)
   - Layout: wrap all buttons in a `flex items-center gap-2` container
   - Build URL inside click handler (not at render time) for SSR safety — `window.location.href` must only be accessed in the event handler

Note: Do NOT add the star rating i18n keys here — those belong to Plan 19-05's scope and will be added to these same i18n files in the next plan.
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm typecheck</automated>
  </verify>
  <done>ShareResultButton renders X and LinkedIn share links alongside the existing copy button. i18n types, ko.ts, and en.ts all include shareOnX and shareOnLinkedin keys. pnpm typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate NewsletterForm into /guides pages</name>
  <files>app/guides/page.tsx, app/guides/[slug]/GuideDetailClient.tsx</files>
  <action>
1. In `app/guides/page.tsx` (client component):
   - Import NewsletterForm: `import NewsletterForm from "@/components/NewsletterForm";`
   - Add a newsletter section after the guide cards grid (before closing `</main>`), matching the same pattern used in `app/page.tsx`:
     ```tsx
     {/* Newsletter */}
     <section className="mt-16 mb-8 rounded-2xl border border-border bg-muted/30 px-6 py-10">
       <NewsletterForm />
     </section>
     ```

2. In `app/guides/[slug]/GuideDetailClient.tsx` (client component):
   - Import NewsletterForm: `import NewsletterForm from "@/components/NewsletterForm";`
   - Add a newsletter section after the guide sections (before closing `</main>`), with the same wrapper styling:
     ```tsx
     {/* Newsletter */}
     <section className="mt-16 mb-8 rounded-2xl border border-border bg-muted/30 px-6 py-10">
       <NewsletterForm />
     </section>
     ```

Both pages are already 'use client' and use `useI18n()`, so NewsletterForm (also 'use client') will work without any additional provider wiring. The I18nProvider wrapping is already handled at the layout level.
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>NewsletterForm renders on both /guides and /guides/[slug] pages. Build passes with no errors.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` — all i18n type extensions compile correctly
2. `pnpm build` — no build errors from new imports or component integration
3. `pnpm test` — existing tests still pass (no test changes in this plan)
4. grep for `twitter.com/intent/tweet` in ShareResultButton.tsx returns a match
5. grep for `linkedin.com/sharing` in ShareResultButton.tsx returns a match
6. grep for `NewsletterForm` in app/guides/page.tsx returns a match
7. grep for `NewsletterForm` in app/guides/[slug]/GuideDetailClient.tsx returns a match
</verification>

<success_criteria>
- ShareResultButton shows X and LinkedIn direct share links for desktop users
- /guides page renders NewsletterForm section below guide cards
- /guides/[slug] page renders NewsletterForm section below guide content
- All i18n types, ko.ts, en.ts include new share keys
- pnpm typecheck && pnpm build && pnpm test all pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-share-feedback-newsletter/19-04-SUMMARY.md`
</output>
