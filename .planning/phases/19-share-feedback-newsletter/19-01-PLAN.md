---
phase: 19-share-feedback-newsletter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/i18n/types.ts
  - lib/i18n/ko.ts
  - lib/i18n/en.ts
  - lib/analytics.ts
  - lib/share-utils.ts
  - lib/__tests__/share-utils.test.ts
autonomous: true
requirements:
  - SHAR-03

must_haves:
  truths:
    - "i18n types.ts에 share, feedback, newsletter 섹션이 정의되어 있다"
    - "ko.ts와 en.ts 모두 새 섹션의 번역 키를 구현한다"
    - "analytics.ts EventName에 5개 새 이벤트 리터럴이 추가되어 있다"
    - "shareResult() 함수가 Web Share API 지원 시 native, 미지원 시 clipboard 결과를 반환한다"
    - "shareResult() 유닛 테스트가 native/clipboard/error 세 경로 모두 커버한다"
  artifacts:
    - path: "lib/i18n/types.ts"
      provides: "share, feedback, newsletter 타입 섹션"
      contains: "share:"
    - path: "lib/i18n/ko.ts"
      provides: "한국어 번역 키"
      contains: "share:"
    - path: "lib/i18n/en.ts"
      provides: "영어 번역 키"
      contains: "share:"
    - path: "lib/analytics.ts"
      provides: "5개 새 EventName 리터럴"
      contains: "result_share"
    - path: "lib/share-utils.ts"
      provides: "shareResult() 함수 + ShareOutcome 타입"
      exports: ["shareResult", "ShareOutcome"]
    - path: "lib/__tests__/share-utils.test.ts"
      provides: "shareResult 유닛 테스트"
      min_lines: 30
  key_links:
    - from: "lib/i18n/ko.ts"
      to: "lib/i18n/types.ts"
      via: "Translations type implementation"
      pattern: "share:"
    - from: "lib/share-utils.ts"
      to: "navigator.share / navigator.clipboard"
      via: "browser API calls"
      pattern: "navigator\\.share|navigator\\.clipboard"
---

<objective>
Phase 19의 공유/피드백/뉴스레터 기능에 필요한 타입 기반을 구축한다. i18n에 3개 새 섹션(share, feedback, newsletter)을 추가하고, analytics에 5개 새 이벤트를 등록하고, share-utils.ts 유틸리티 함수를 TDD로 구현한다.

Purpose: 이후 Plan 19-02(ShareResultButton)과 19-03(FeedbackWidget, NewsletterForm)이 참조할 타입과 유틸리티를 먼저 확보하여, 후속 플랜에서 타입 에러 없이 즉시 구현에 들어갈 수 있게 한다.
Output: i18n 타입/번역 확장, analytics 이벤트 확장, share-utils.ts + 테스트
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-share-feedback-newsletter/19-RESEARCH.md

@lib/i18n/types.ts
@lib/i18n/ko.ts
@lib/i18n/en.ts
@lib/analytics.ts

<interfaces>
<!-- Existing i18n type structure (from lib/i18n/types.ts) -->
<!-- Executor must ADD new sections to the existing Translations type, not replace it -->

From lib/i18n/types.ts:
```typescript
export type Locale = "ko" | "en";
export type Translations = {
  nav: { ... };
  main: { ... };
  input: { ... };
  analysis: { ... };
  // ... 12 existing sections ...
  optimizer: { ... };
};
```

From lib/analytics.ts:
```typescript
type EventName =
  | "landing_view"
  | "analysis_start"
  // ... 14 more existing events ...
  | "plugin_suggestion_submit";

type EventPayload = Record<string, string | number | boolean>;
export function trackEvent(event: EventName, payload: EventPayload = {}): void;
```

From lib/rate-limit.ts:
```typescript
type RateLimitOptions = {
  name: string;
  maxRequests: number;
  windowMs: number;
};
export function checkRateLimit(request: NextRequest, options: RateLimitOptions): { allowed: boolean; remaining: number; resetAt: number };
export function cleanupExpiredEntries(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n 타입 확장 + 번역 키 추가 + analytics EventName 확장</name>
  <files>lib/i18n/types.ts, lib/i18n/ko.ts, lib/i18n/en.ts, lib/analytics.ts</files>
  <behavior>
    - types.ts의 Translations 타입에 share, feedback, newsletter 3개 섹션이 존재한다
    - ko.ts가 Translations 타입을 만족한다 (pnpm typecheck 통과)
    - en.ts가 Translations 타입을 만족한다 (pnpm typecheck 통과)
    - analytics.ts EventName에 result_share, feedback_open, feedback_submit, newsletter_subscribe, newsletter_error 5개 리터럴이 존재한다
  </behavior>
  <action>
1. **lib/i18n/types.ts** — `Translations` 타입의 마지막 섹션(optimizer) 뒤에 3개 새 섹션 추가:
```typescript
// Share
share: {
  button: string;
  copied: string;
  title: string;
};
// Feedback
feedback: {
  buttonLabel: string;
  panelTitle: string;
  typeBug: string;
  typeFeature: string;
  typeOther: string;
  messagePlaceholder: string;
  submit: string;
  submitting: string;
  successMsg: string;
  errorMsg: string;
  close: string;
};
// Newsletter
newsletter: {
  sectionTitle: string;
  sectionDesc: string;
  placeholder: string;
  submit: string;
  submitting: string;
  successMsg: string;
  errorMsg: string;
  privacyNote: string;
};
```

2. **lib/i18n/ko.ts** — `optimizer` 섹션 뒤에 한국어 번역 추가:
```typescript
share: {
  button: "공유하기",
  copied: "복사됨",
  title: "Plugin Advisor 추천 결과",
},
feedback: {
  buttonLabel: "피드백",
  panelTitle: "의견을 알려주세요",
  typeBug: "버그 신고",
  typeFeature: "기능 제안",
  typeOther: "기타",
  messagePlaceholder: "어떤 점이 불편하거나 개선되면 좋겠나요?",
  submit: "보내기",
  submitting: "전송 중...",
  successMsg: "소중한 의견 감사합니다!",
  errorMsg: "전송에 실패했습니다. 잠시 후 다시 시도해 주세요.",
  close: "닫기",
},
newsletter: {
  sectionTitle: "새 플러그인 소식 받기",
  sectionDesc: "새 플러그인 업데이트와 활용 팁을 이메일로 전달해 드립니다.",
  placeholder: "이메일 주소",
  submit: "구독하기",
  submitting: "구독 중...",
  successMsg: "구독이 완료되었습니다!",
  errorMsg: "구독에 실패했습니다. 잠시 후 다시 시도해 주세요.",
  privacyNote: "스팸 없이, 새 플러그인 소식만 보내드립니다.",
},
```

3. **lib/i18n/en.ts** — 동일 구조의 영어 번역:
```typescript
share: {
  button: "Share",
  copied: "Copied",
  title: "Plugin Advisor Results",
},
feedback: {
  buttonLabel: "Feedback",
  panelTitle: "Send us feedback",
  typeBug: "Bug report",
  typeFeature: "Feature request",
  typeOther: "Other",
  messagePlaceholder: "What could we improve?",
  submit: "Submit",
  submitting: "Submitting...",
  successMsg: "Thank you for your feedback!",
  errorMsg: "Failed to send. Please try again later.",
  close: "Close",
},
newsletter: {
  sectionTitle: "Get plugin updates",
  sectionDesc: "We'll send you new plugin updates and tips via email.",
  placeholder: "Email address",
  submit: "Subscribe",
  submitting: "Subscribing...",
  successMsg: "You're subscribed!",
  errorMsg: "Subscription failed. Please try again later.",
  privacyNote: "No spam, just new plugin updates.",
},
```

4. **lib/analytics.ts** — `EventName` 유니온의 마지막 라인(`"plugin_suggestion_submit"`) 뒤에 5개 추가:
```typescript
| "result_share"
| "feedback_open"
| "feedback_submit"
| "newsletter_subscribe"
| "newsletter_error";
```
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm typecheck</automated>
  </verify>
  <done>types.ts에 share/feedback/newsletter 섹션이 정의되고, ko.ts/en.ts 모두 구현하여 typecheck 통과. analytics.ts에 5개 새 EventName이 추가됨.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: share-utils.ts 유틸리티 + 유닛 테스트 (TDD)</name>
  <files>lib/share-utils.ts, lib/__tests__/share-utils.test.ts</files>
  <behavior>
    - shareResult(url) — navigator.share 사용 가능 시 navigator.share({url, title}) 호출 후 "native" 반환
    - shareResult(url) — navigator.share 미지원 시 navigator.clipboard.writeText(url) 호출 후 "clipboard" 반환
    - shareResult(url) — navigator.share가 AbortError(사용자 취소) throw 시 "error" 반환
    - shareResult(url) — navigator/clipboard 모두 실패 시 "error" 반환
    - ShareOutcome 타입이 "native" | "clipboard" | "error" 로 export 됨
  </behavior>
  <action>
**RED phase — 테스트 먼저 작성 (lib/__tests__/share-utils.test.ts):**

```typescript
import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
import { shareResult } from "../share-utils";
import type { ShareOutcome } from "../share-utils";

describe("shareResult", () => {
  const originalNavigator = global.navigator;

  afterEach(() => {
    vi.restoreAllMocks();
    // Restore navigator
    Object.defineProperty(global, "navigator", {
      value: originalNavigator,
      writable: true,
      configurable: true,
    });
  });

  it('returns "native" when navigator.share succeeds', async () => {
    const shareMock = vi.fn().mockResolvedValue(undefined);
    Object.defineProperty(global, "navigator", {
      value: { share: shareMock },
      writable: true,
      configurable: true,
    });
    const result = await shareResult("https://example.com", "Title");
    expect(result).toBe("native");
    expect(shareMock).toHaveBeenCalledWith({ url: "https://example.com", title: "Title" });
  });

  it('returns "clipboard" when no share API and clipboard succeeds', async () => {
    const writeTextMock = vi.fn().mockResolvedValue(undefined);
    Object.defineProperty(global, "navigator", {
      value: { clipboard: { writeText: writeTextMock } },
      writable: true,
      configurable: true,
    });
    const result = await shareResult("https://example.com");
    expect(result).toBe("clipboard");
    expect(writeTextMock).toHaveBeenCalledWith("https://example.com");
  });

  it('returns "error" when navigator.share throws (user dismissed)', async () => {
    const shareMock = vi.fn().mockRejectedValue(new DOMException("Abort", "AbortError"));
    Object.defineProperty(global, "navigator", {
      value: { share: shareMock },
      writable: true,
      configurable: true,
    });
    const result = await shareResult("https://example.com");
    expect(result).toBe("error");
  });

  it('returns "error" when both share and clipboard fail', async () => {
    Object.defineProperty(global, "navigator", {
      value: {},
      writable: true,
      configurable: true,
    });
    const result = await shareResult("https://example.com");
    expect(result).toBe("error");
  });
});
```

`pnpm test -- share-utils`로 실행하여 RED 확인 (share-utils.ts 없으므로 import 실패).

**GREEN phase — lib/share-utils.ts 구현:**

```typescript
export type ShareOutcome = "native" | "clipboard" | "error";

export async function shareResult(url: string, title?: string): Promise<ShareOutcome> {
  try {
    if (typeof navigator !== "undefined" && "share" in navigator) {
      await navigator.share({ url, title });
      return "native";
    }
    if (typeof navigator !== "undefined" && navigator.clipboard) {
      await navigator.clipboard.writeText(url);
      return "clipboard";
    }
    return "error";
  } catch {
    return "error";
  }
}
```

`pnpm test -- share-utils`로 실행하여 GREEN 확인 (4개 테스트 모두 통과).

**주의사항:**
- `navigator` 접근은 반드시 함수 본문 내에서 (모듈 최상위 X — SSR 안전)
- `'share' in navigator` 체크로 Web Share API 감지 (typeof check가 아닌 in operator 사용)
- clipboard fallback에서 `navigator.clipboard` 존재 여부도 체크 (일부 HTTP 환경에서 undefined)
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm test -- share-utils</automated>
  </verify>
  <done>share-utils.ts가 ShareOutcome 타입과 shareResult() 함수를 export하고, 4개 유닛 테스트가 native/clipboard/error 경로를 모두 검증하며 통과함.</done>
</task>

</tasks>

<verification>
pnpm typecheck && pnpm test -- share-utils && pnpm lint
</verification>

<success_criteria>
- pnpm typecheck 통과 (i18n types + ko/en 일치, analytics EventName 확장)
- pnpm test -- share-utils 통과 (4개 테스트 GREEN)
- lib/share-utils.ts에서 ShareOutcome, shareResult가 export됨
- lib/i18n/types.ts에 share, feedback, newsletter 섹션이 정의됨
- lib/analytics.ts에 result_share, feedback_open, feedback_submit, newsletter_subscribe, newsletter_error가 추가됨
</success_criteria>

<output>
After completion, create `.planning/phases/19-share-feedback-newsletter/19-01-SUMMARY.md`
</output>
