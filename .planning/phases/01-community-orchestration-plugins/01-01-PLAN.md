---
phase: 01-community-orchestration-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
  - VERIFY-05
  - VERIFY-06

must_haves:
  truths:
    - "omc, agency-agents, ralph 3개 GitHub repo가 존재하고 활성 상태임이 확인되었다"
    - "3개 플러그인의 desc, longDesc가 README 내용과 일치하도록 수정되었다"
    - "3개 플러그인의 features가 실제 기능과 일치하도록 수정되었다"
    - "3개 플러그인의 keywords가 실제 용도를 반영하도록 수정되었다"
    - "3개 플러그인의 install 명령어가 공식 설치 방법과 일치한다"
    - "conflicts 규칙이 실제 호환성과 일치한다 (omc <-> superpowers 충돌 포함)"
    - "3개 플러그인의 verificationStatus가 verified로 업데이트되었다"
    - "영문 번역(plugins-en.ts)이 수정된 한국어 내용과 동기화되었다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "omc, agency-agents, ralph 플러그인 메타데이터 (검증 완료)"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "omc, agency-agents, ralph 영문 번역 (동기화 완료)"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "동일 plugin ID 키로 desc/longDesc 매핑"
      pattern: "omc.*desc|agency-agents.*desc|ralph.*desc"
---

<objective>
omc(Yeachan-Heo/oh-my-claudecode), agency-agents(msitarzewski/agency-agents), ralph(haizelabs/ralph-wiggum) 3개 커뮤니티 오케스트레이션 플러그인의 메타데이터를 GitHub repo README와 대조해서 검증하고 수정한다.

Purpose: 플러그인 추천 서비스가 정확한 정보를 기반으로 추천하려면 메타데이터가 실제와 일치해야 한다.
Output: 검증 완료된 plugins.ts와 동기화된 plugins-en.ts
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
@lib/types.ts

<interfaces>
<!-- Plugin type from lib/types.ts -->
```typescript
export type Plugin = {
  id: string;
  name: string;
  tag: string;
  color: string;
  desc: string;
  longDesc: string;
  url: string;
  githubRepo: string | null;
  category: PluginCategory;
  install: string[];
  features: string[];
  conflicts: string[];
  keywords: string[];
  officialStatus: OfficialStatus;
  verificationStatus: VerificationStatus;
  difficulty: PluginDifficulty;
  prerequisites: string[];
  requiredSecrets: string[];
  platformSupport: PlatformSupport[];
  installMode: InstallMode;
  maintenanceStatus: MaintenanceStatus;
  bestFor: string[];
  avoidFor: string[];
};
```

<!-- Current plugin data to verify (from lib/plugins.ts CORE_PLUGINS) -->

omc:
- githubRepo: "Yeachan-Heo/oh-my-claudecode"
- desc: "32개 전문 에이전트, 멀티에이전트 오케스트레이션. 복잡한 장기 프로젝트의 핵심."
- install: ["/plugin marketplace add https://github.com/Yeachan-Heo/oh-my-claudecode", "/plugin install oh-my-claudecode", "/oh-my-claudecode:omc-setup"]
- features: ["32개 전문 에이전트", "자율 실행 모드", "Codex/Gemini 연동", "토큰 최적화 라우팅"]
- conflicts: ["superpowers"]
- verificationStatus: "partial" (in PLUGIN_FIELD_OVERRIDES)

agency-agents:
- githubRepo: "msitarzewski/agency-agents"
- desc: "프론트엔드, 백엔드, 마케팅, PM 등 역할별 AI 전문가 에이전트 묶음..."
- install: ["git clone ...", "cd agency-agents", "./scripts/install.sh --tool claude-code"]
- features: ["61개+ 전문 에이전트", "역할별 페르소나", "멀티툴 설치 스크립트", "마케팅/PM/디자인까지 확장"]
- conflicts: []
- verificationStatus: "partial" (in PLUGIN_FIELD_OVERRIDES)

ralph:
- githubRepo: "haizelabs/ralph-wiggum"
- desc: "자율 코딩 루프. PRD 완료까지 반복 실행 후 git 커밋 자동 처리."
- install: ["/plugin marketplace add https://github.com/haizelabs/ralph-wiggum", "/plugin install ralph-wiggum"]
- features: ["자율 반복 실행", "자동 git 커밋", "완료 검증", "PRD 기반 루프"]
- conflicts: []
- verificationStatus: "partial" (default, no override)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch GitHub READMEs and verify all metadata for omc, agency-agents, ralph</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 3 plugins, fetch the GitHub README and compare against current metadata in lib/plugins.ts. Then apply ALL corrections in a single pass.

**Step 1: Fetch READMEs**
Use WebFetch to read these 3 URLs (all 3 can be fetched in parallel):
- https://github.com/Yeachan-Heo/oh-my-claudecode (omc)
- https://github.com/msitarzewski/agency-agents (agency-agents)
- https://github.com/haizelabs/ralph-wiggum (ralph)

For each README, extract:
1. **Repo status** (VERIFY-01): Does the repo exist? Last commit date? Is it actively maintained?
2. **Description accuracy** (VERIFY-02): Compare README's project description against current `desc` and `longDesc`. Note any inaccuracies or outdated claims.
3. **Features accuracy** (VERIFY-03): Compare README's feature list against current `features` array. Note missing, incorrect, or outdated features.
4. **Keywords accuracy** (VERIFY-04): Based on README content, are current `keywords` appropriate? Note any missing important keywords or irrelevant ones.
5. **Install commands** (VERIFY-05): Compare README's installation instructions against current `install` array. Pay special attention to:
   - omc: Check if `/plugin marketplace add` format is correct, or if it uses a different install method
   - agency-agents: Verify the git clone + install.sh flow, check if `--tool claude-code` flag exists
   - ralph: Verify the plugin marketplace install format
6. **Conflicts** (VERIFY-06): Check if README mentions any incompatibilities. Verify omc's `conflicts: ["superpowers"]` is still valid.

**Step 2: Apply corrections to lib/plugins.ts**
In the CORE_PLUGINS object, update each plugin's fields based on findings:
- Update `desc` and `longDesc` if README shows different/more accurate descriptions
- Update `features` array to match actual features from README
- Update `keywords` array if README reveals important missing keywords
- Update `install` array to match README's actual installation instructions
- Update `conflicts` array if findings differ
- Keep the same Korean writing style for desc/longDesc (concise, friendly tone with -요 endings)

In PLUGIN_FIELD_OVERRIDES, for each of the 3 plugins:
- Change `verificationStatus` from `"partial"` to `"verified"`
- Update `bestFor`, `avoidFor`, `prerequisites` if README provides better information
- For ralph: add an explicit override entry with `verificationStatus: "verified"` (currently has no override, defaults to "partial")

**Step 3: Sync English translations in lib/i18n/plugins-en.ts**
Update `pluginDescEn` entries for omc, agency-agents, ralph:
- Translate the updated Korean `desc` and `longDesc` to English
- Match the same structure and tone as existing English entries

Update `reasonsEn` entries for omc, agency-agents, ralph if the recommendation reason needs updating based on new descriptions.

**Important constraints:**
- Do NOT modify any plugin data other than omc, agency-agents, ralph
- Do NOT change the code structure (CORE_PLUGINS, PLUGIN_FIELD_OVERRIDES, etc.)
- Preserve existing TypeScript types and patterns
- Keep Korean UI text style consistent with existing entries
- If a README is unavailable or repo appears dead, set `verificationStatus` to `"unverified"` instead of `"verified"` and document why in a code comment
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- All 3 GitHub repos confirmed active or status documented
- desc, longDesc for omc/agency-agents/ralph updated to match README
- features arrays updated to match actual README feature lists
- keywords arrays reviewed and updated based on README content
- install commands match README installation instructions
- conflicts validated (omc <-> superpowers confirmed or corrected)
- verificationStatus updated to "verified" (or "unverified" with reason)
- English translations in plugins-en.ts synced with updated Korean text
- TypeScript compiles, lint passes, build succeeds
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes — no type errors from plugin data changes
2. `pnpm lint` passes — no lint issues
3. `pnpm build` passes — production build succeeds
4. In lib/plugins.ts, verify:
   - omc, agency-agents, ralph entries have been updated
   - PLUGIN_FIELD_OVERRIDES has verificationStatus: "verified" for all 3
5. In lib/i18n/plugins-en.ts, verify:
   - pluginDescEn has updated desc/longDesc for all 3
   - reasonsEn entries are consistent with updated descriptions
</verification>

<success_criteria>
- 3개 플러그인(omc, agency-agents, ralph)의 모든 메타데이터가 GitHub README와 일치한다
- verificationStatus가 "verified"로 업데이트되었다 (또는 repo 문제 시 "unverified" + 사유)
- 영문 번역이 수정된 한국어와 동기화되었다
- TypeScript 컴파일, 린트, 빌드 모두 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/01-community-orchestration-plugins/01-01-SUMMARY.md`
</output>
