---
phase: 10-scoring-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoring.ts
  - lib/__tests__/scoring.test.ts
  - components/OptimizerApp.tsx
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03

must_haves:
  truths:
    - "scorePlugins('mcp') 호출 시 complements에 plugin 타입 항목이 포함되지 않는다"
    - "scorePlugins('plugin') 호출 시 complements에 mcp 타입 항목이 포함되지 않는다"
    - "scorePlugins('both') 호출 시 기존과 동일하게 모든 타입이 포함된다"
    - "typeScope 없이 호출 시 기본값 'both'로 동작하여 기존 테스트가 회귀 없이 통과한다"
    - "MCP 전용 입력의 score 값이 typeScope 적용 전후로 동일하다"
    - "OptimizerApp에서 선택된 플러그인의 type 분포에 따라 typeScope이 자동 결정된다"
  artifacts:
    - path: "lib/scoring.ts"
      provides: "scorePlugins with typeScope parameter, ScoringResult.typeScope field"
      contains: "typeScope"
    - path: "lib/__tests__/scoring.test.ts"
      provides: "typeScope filtering regression tests"
      contains: "typeScope filtering"
    - path: "components/OptimizerApp.tsx"
      provides: "typeScope detection from selectedPlugins type distribution"
      contains: "typeScope"
  key_links:
    - from: "components/OptimizerApp.tsx"
      to: "lib/scoring.ts"
      via: "scorePlugins(selectedIds, typeScope) call"
      pattern: "scorePlugins\\(selectedIds,\\s*typeScope\\)"
    - from: "lib/scoring.ts"
      to: "lib/plugins.ts"
      via: "plugin.type === typeScope filter in buildComplements and buildReplacements"
      pattern: "plugin\\.type === typeScope"
---

<objective>
scorePlugins 함수에 typeScope 파라미터를 추가하여 MCP/Plugin별 보완 추천(complements)과 대체 추천(replacements)을 분리한다. OptimizerApp 호출자에서 typeScope을 자동 결정한다.

Purpose: Phase 9에서 Plugin 타입 13개가 DB에 추가되어 MCP 전용 분석에도 Plugin 보완 추천이 나타나는 문제를 해결한다.
Output: typeScope 파라미터가 추가된 scoring.ts, 회귀 테스트, OptimizerApp 호출자 업데이트
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scoring-extension/10-CONTEXT.md
@.planning/phases/10-scoring-extension/10-RESEARCH.md

@lib/scoring.ts
@lib/types.ts
@lib/__tests__/scoring.test.ts
@components/OptimizerApp.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/types.ts:
```typescript
export type ItemType = "mcp" | "plugin";

export type Plugin = {
  id: string;
  name: string;
  category: PluginCategory;
  type: ItemType;
  verificationStatus: VerificationStatus;
  maintenanceStatus: MaintenanceStatus;
  // ... other fields
};
```

From lib/scoring.ts (CURRENT — will be modified):
```typescript
export type ScoringResult = {
  empty: boolean;
  score: number | null;
  conflicts: ConflictWarning[];
  redundancies: RedundancyGroup[];
  coverage: CoverageResult;
  complements: ComplementSuggestion[];
  replacements: ReplacementSuggestion[];
};

export function scorePlugins(ids: string[]): ScoringResult;
```

From lib/plugins.ts:
```typescript
export const PLUGINS: Record<string, Plugin>;
// 42 MCP entries (type: 'mcp') + 13 Plugin entries (type: 'plugin')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typeScope tests and implement scoring.ts changes</name>
  <files>lib/__tests__/scoring.test.ts, lib/scoring.ts</files>
  <behavior>
    - Test: scorePlugins returns typeScope field in result (SCORE-01)
    - Test: scorePlugins defaults typeScope to 'both' when not specified (SCORE-01)
    - Test: mcp typeScope — complements contain only mcp-type plugins (SCORE-02)
    - Test: plugin typeScope — complements contain only plugin-type plugins (SCORE-02)
    - Test: mcp typeScope — replacements contain only mcp-type alternatives (SCORE-02)
    - Test: both typeScope — complements can include any type (SCORE-02)
    - Test: mcp typeScope score equals both typeScope score for MCP-only input (SCORE-03)
    - Test: empty input returns typeScope in result (SCORE-01)
    - Test: existing tests pass without modification (backward compat via default 'both')
  </behavior>
  <action>
RED phase — Add new describe block "typeScope filtering" to lib/__tests__/scoring.test.ts:

1. Import PLUGINS from lib/plugins at the top of the test file (needed to verify plugin types).

2. Add describe block with these tests:
   - `returns typeScope in result when specified`: `scorePlugins(["context7"], "mcp")` -> `result.typeScope === "mcp"`
   - `defaults typeScope to 'both' when not specified`: `scorePlugins(["context7"])` -> `result.typeScope === "both"`
   - `empty input returns typeScope`: `scorePlugins([], "mcp")` -> `result.typeScope === "mcp"`
   - `mcp typeScope: complements contain only mcp-type plugins`: `scorePlugins(["context7"], "mcp")` -> every `result.complements[*].pluginId` resolves to `PLUGINS[id].type === "mcp"`
   - `plugin typeScope: complements contain only plugin-type plugins`: `scorePlugins(["omc"], "plugin")` -> every `result.complements[*].pluginId` resolves to `PLUGINS[id].type === "plugin"`
   - `mcp typeScope: replacements suggest only mcp-type alternatives`: `scorePlugins(["linear"], "mcp")` -> every non-null `result.replacements[*].replacement` resolves to `PLUGINS[id].type === "mcp"` (linear is stale mcp, should only suggest mcp replacements)
   - `both typeScope: complements can include any type`: `scorePlugins(["context7"], "both")` -> `result.complements.length >= 0` (no type restriction)
   - `mcp typeScope: score unchanged from both for MCP-only input`: `scorePlugins(["context7"], "mcp").score === scorePlugins(["context7"], "both").score`

3. Run tests — they MUST fail (RED). The scorePlugins signature does not accept typeScope yet.

GREEN phase — Modify lib/scoring.ts:

1. Add `ItemType` to the import from `./types`:
   ```typescript
   import type { PluginCategory, Plugin, ItemType } from "./types";
   ```

2. Extend `ScoringResult` type — add `typeScope: ItemType | 'both'` field.

3. Change `scorePlugins` signature:
   ```typescript
   export function scorePlugins(
     ids: string[],
     typeScope: ItemType | 'both' = 'both'
   ): ScoringResult
   ```

4. In the empty return branch (ids.length === 0), add `typeScope` field to the return object.

5. Change `buildComplements` signature to accept `typeScope: ItemType | 'both'` as third parameter. Inside the candidates filter, add condition: `(typeScope === 'both' || plugin.type === typeScope)`.

6. Change `buildReplacements` signature to accept `typeScope: ItemType | 'both'` as second parameter. Inside the alternatives filter, add condition: `(typeScope === 'both' || p.type === typeScope)`.

7. Update `scorePlugins` body to pass `typeScope` to `buildComplements(coverage.uncovered, ids, typeScope)` and `buildReplacements(ids, typeScope)`.

8. Add `typeScope` field to the non-empty return object.

9. Run tests — all MUST pass (GREEN), including all existing tests (backward compat via default 'both').

IMPORTANT: Do NOT modify buildCoverage or calculateScore — per locked decision.
IMPORTANT: Do NOT add type detection logic inside scorePlugins — per locked decision, scorePlugins is pure scoring only.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm test -- lib/__tests__/scoring.test.ts</automated>
  </verify>
  <done>
    - ScoringResult type has typeScope field (SCORE-01)
    - scorePlugins accepts optional typeScope parameter defaulting to 'both' (SCORE-01)
    - mcp typeScope filters complements to mcp-only (SCORE-02)
    - plugin typeScope filters complements to plugin-only (SCORE-02)
    - mcp typeScope filters replacement alternatives to mcp-only (SCORE-02)
    - Score value unchanged between typeScope='mcp' and 'both' for MCP-only input (SCORE-03)
    - All existing tests pass without modification (backward compat)
    - New typeScope describe block with 8+ tests all green
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire OptimizerApp caller to determine and pass typeScope</name>
  <files>components/OptimizerApp.tsx</files>
  <action>
Modify components/OptimizerApp.tsx to determine typeScope from selected plugins and pass it to scorePlugins.

1. Add `ItemType` to the import from `@/lib/types`:
   ```typescript
   import type { Plugin, ItemType } from "@/lib/types";
   ```

2. In the `handleAnalyze` callback, before the `scorePlugins` call, add typeScope detection:
   ```typescript
   const types = new Set(selectedPlugins.map((p) => p.type));
   const typeScope: ItemType | 'both' =
     types.size === 1
       ? (types.values().next().value as ItemType)
       : 'both';
   ```

3. Update the scorePlugins call to pass typeScope:
   ```typescript
   const scored = scorePlugins(selectedIds, typeScope);
   ```

4. Note: The `selectedIds` variable is declared AFTER `handleAnalyze` in the current code (line 77). The `handleAnalyze` callback captures `selectedPlugins` via closure (line 75 deps array), and `selectedIds` is derived from `selectedPlugins` inside the callback's setTimeout. Verify the variable scoping: `selectedIds` on line 70 references the outer `selectedIds` on line 77. This works because `selectedIds` is in scope when handleAnalyze executes (not when it's defined). But to be safe, derive selectedIds from selectedPlugins inside the setTimeout:
   ```typescript
   setTimeout(() => {
     const ids = selectedPlugins.map((p) => p.id);
     const types = new Set(selectedPlugins.map((p) => p.type));
     const typeScope: ItemType | 'both' =
       types.size === 1
         ? (types.values().next().value as ItemType)
         : 'both';
     const scored = scorePlugins(ids, typeScope);
     setResult(scored);
     setAnalysisState("done");
   }, 0);
   ```

5. Run typecheck and build to verify no type errors.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm test -- lib/__tests__/scoring.test.ts</automated>
  </verify>
  <done>
    - OptimizerApp determines typeScope from selectedPlugins type distribution
    - MCP-only selection passes typeScope='mcp' to scorePlugins
    - Plugin-only selection passes typeScope='plugin' to scorePlugins
    - Mixed selection passes typeScope='both' to scorePlugins
    - pnpm typecheck passes with no errors
    - All scoring tests still pass
  </done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. `pnpm typecheck` — no type errors
2. `pnpm lint` — no lint errors
3. `pnpm test` — all tests pass (existing + new typeScope tests)
4. `pnpm build` — successful build

Manual spot-check (optional, not blocking):
- Run dev server, paste MCP-only list, verify complements are MCP-only
- Run dev server, type-select plugin-only items, verify complements are plugin-only
</verification>

<success_criteria>
1. scorePlugins에 typeScope 파라미터가 추가되어 'mcp' | 'plugin' | 'both' 중 하나를 받는다 (SCORE-01)
2. MCP 목록만 입력 시 보완/대체 추천에 Plugin 타입 항목이 나타나지 않는다 (SCORE-02)
3. Plugin 목록만 입력 시 보완/대체 추천에 MCP 타입 항목이 나타나지 않는다 (SCORE-02)
4. 기존 MCP 전용 유저의 조합 점수가 Plugin DB 추가 전후로 동일하게 유지된다 (SCORE-03)
5. typeScope 관련 회귀 테스트가 추가되어 pnpm test가 통과한다 (SCORE-01/02/03)
6. pnpm typecheck && pnpm build가 오류 없이 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/10-scoring-extension/10-01-SUMMARY.md`
</output>
