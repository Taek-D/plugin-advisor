---
phase: 15-verification-test-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/__tests__/plugins.test.ts
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
  - VER-04

must_haves:
  truths:
    - "신규 9개 항목의 install 명령이 STATE.md의 검증 결정과 일치하며, 소스 코드(lib/plugins.ts)에 정확히 반영되어 있다"
    - "신규 9개 항목 모두 pluginDescEn과 reasonsEn에 desc, longDesc, reason 번역이 존재한다"
    - "plugins.test.ts의 카운트 임계값이 51로 업데이트되고 pnpm test가 통과한다 (REQUIREMENTS.md VER-03 기준: 42 + 9 = 51)"
    - "pnpm typecheck, lint, build, test가 모두 에러 없이 통과한다"
  artifacts:
    - path: "lib/__tests__/plugins.test.ts"
      provides: "Updated threshold sanity check"
      contains: "toBeGreaterThanOrEqual(51)"
  key_links:
    - from: "lib/__tests__/plugins.test.ts"
      to: "lib/plugins.ts"
      via: "import PLUGINS"
      pattern: "Object\\.keys\\(PLUGINS\\)\\.length"
---

<objective>
Verify data accuracy of the 9 new plugin entries added in Phases 13-14, update the test threshold to reflect the current DB size (51), and confirm the full CI pipeline passes.

Purpose: Final quality gate for v1.3 milestone — ensures all new data is accurate and the codebase is ship-ready.
Output: Updated plugins.test.ts with threshold 51, documented verification evidence, green CI.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-verification-test-update/15-RESEARCH.md
@lib/__tests__/plugins.test.ts
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify install commands and i18n completeness (VER-01 + VER-02)</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
Source-level verification of all 9 new entries. No files are modified — this is a read-and-confirm task.

**VER-01 — Install command verification (manual read + grep):**
For each of the 9 IDs below, read the `install` array in `CORE_PLUGINS` (lib/plugins.ts) and confirm it matches the STATE.md verified decision. Use grep to extract and log the exact install string for each entry:

1. fetch: must contain `uvx mcp-fetch`
2. time: must contain `uvx mcp-server-time`
3. markitdown: must contain `pip install markitdown-mcp` (no claude mcp add)
4. magic-mcp: installMode=external-setup, requiredSecrets includes TWENTY_FIRST_API_KEY
5. shadcn-mcp: must contain `pnpm dlx shadcn@latest mcp init --client claude` (not npx)
6. n8n-mcp: installMode=safe-copy, MCP_MODE=stdio
7. claude-mem: install via /plugin marketplace add + /plugin install (not npm install -g)
8. superclaude: install ID = sc@SuperClaude-Org
9. frontend-design: source = anthropics/claude-code monorepo

For each entry, grep the install array from lib/plugins.ts and log the exact match. This is read-only verification — confirmation is documented in the SUMMARY.

**VER-02 — i18n sync verification (automated grep):**
For each of the 9 IDs, confirm in lib/i18n/plugins-en.ts:
- pluginDescEn[id].desc exists and is non-empty
- pluginDescEn[id].longDesc exists and is non-empty
- reasonsEn[id] exists and is non-empty

Use grep to extract each key and confirm presence. Log confirmation for all 9.

Do NOT modify any files. This task produces verification evidence only.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && echo "=== VER-02: i18n completeness ===" && for id in fetch time markitdown magic-mcp shadcn-mcp n8n-mcp claude-mem superclaude frontend-design; do grep -q "\"$id\"" lib/i18n/plugins-en.ts && echo "VER-02: $id i18n OK" || echo "VER-02: $id i18n MISSING"; done</automated>
    VER-01 is verified manually by reading lib/plugins.ts install arrays for each of the 9 entries and comparing against STATE.md decisions. The executor must grep and log the following install strings to confirm VER-01:
    - grep "uvx mcp-fetch" lib/plugins.ts
    - grep "uvx mcp-server-time" lib/plugins.ts
    - grep "pip install markitdown-mcp" lib/plugins.ts
    - grep "external-setup" lib/plugins.ts (magic-mcp)
    - grep "pnpm dlx shadcn" lib/plugins.ts
    - grep "safe-copy" lib/plugins.ts (n8n-mcp)
    - grep "marketplace add" lib/plugins.ts (claude-mem)
    - grep "sc@SuperClaude-Org" lib/plugins.ts (superclaude)
    - grep "anthropics/claude-code" lib/plugins.ts (frontend-design)
    Each grep must return a match. Results are documented in the SUMMARY as VER-01 evidence.
  </verify>
  <done>VER-01: All 9 install commands confirmed matching STATE.md decisions via grep evidence logged in SUMMARY. VER-02: All 9 entries have complete pluginDescEn (desc + longDesc) and reasonsEn translations confirmed by automated grep.</done>
</task>

<task type="auto">
  <name>Task 2: Update test threshold and run full CI gate (VER-03 + VER-04)</name>
  <files>lib/__tests__/plugins.test.ts</files>
  <action>
**VER-03 — Update threshold (per REQUIREMENTS.md VER-03: 51):**
Edit lib/__tests__/plugins.test.ts line 52-54. Change:
```typescript
it("PLUGINS object has at least 42 entries (sanity check)", () => {
  expect(Object.keys(PLUGINS).length).toBeGreaterThanOrEqual(42);
});
```
To:
```typescript
it("PLUGINS object has at least 51 entries (sanity check)", () => {
  expect(Object.keys(PLUGINS).length).toBeGreaterThanOrEqual(51);
});
```

The threshold of 51 matches REQUIREMENTS.md VER-03 and the actual DB count (42 original + 9 new = 51).

Do NOT change any other lines. PLUGIN_TYPE_IDS already includes claude-mem, superclaude, frontend-design (confirmed in research).

**VER-04 — Run full CI sequence in order:**
1. `pnpm typecheck` — must pass (runs next build compile + tsc --noEmit)
2. `pnpm lint` — must pass (runs next lint)
3. `pnpm build` — must pass (runs next build)
4. `pnpm test` — must pass (runs vitest run, all tests including updated threshold)

Run these sequentially: typecheck first (catches type errors that cause misleading build failures), then lint, build, test. All four must exit 0.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build && pnpm test</automated>
  </verify>
  <done>plugins.test.ts threshold updated from 42 to 51 (aligned with REQUIREMENTS.md VER-03). pnpm typecheck, lint, build, and test all pass with exit code 0. No regressions.</done>
</task>

</tasks>

<verification>
Phase 15 is complete when:
1. VER-01: All 9 install commands verified against STATE.md decisions (source-level grep evidence in SUMMARY)
2. VER-02: All 9 entries have pluginDescEn.desc, pluginDescEn.longDesc, and reasonsEn entries (automated grep confirms)
3. VER-03: plugins.test.ts threshold is 51 (updated from 42, per REQUIREMENTS.md VER-03) and pnpm test passes
4. VER-04: pnpm typecheck, lint, build, test all green (exit 0)
</verification>

<success_criteria>
- grep "toBeGreaterThanOrEqual(51)" lib/__tests__/plugins.test.ts returns a match
- pnpm typecheck && pnpm lint && pnpm build && pnpm test exits 0
- All 9 new entries verified for install accuracy (VER-01 grep evidence) and i18n completeness (VER-02 automated)
</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-test-update/15-01-SUMMARY.md`
</output>
