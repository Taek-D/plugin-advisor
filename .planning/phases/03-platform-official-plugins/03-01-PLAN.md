---
phase: 03-platform-official-plugins
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "notion, firecrawl, exa, tavily 4개 플러그인의 공식 GitHub repo가 존재하고 활성 상태임이 확인되었다"
    - "4개 플러그인의 install 명령어가 각 공식 README의 Claude Code 설치 방법과 일치한다"
    - "4개 플러그인의 features, keywords가 실제 README에 문서화된 기능을 정확히 반영한다"
    - "4개 플러그인의 requiredSecrets가 실제 설정에 필요한 환경변수명과 일치한다"
    - "tavily의 conflicts: [brave-search]가 유효한 추천 시스템 설계 결정으로 확인되었다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for notion, firecrawl, exa, tavily"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for notion, firecrawl, exa, tavily"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "notion|firecrawl|exa|tavily"
---

<objective>
Verify and update metadata for notion, firecrawl, exa, and tavily plugins against their official GitHub repos.

Purpose: Ensure these 4 standalone official plugins have accurate descriptions, features, install commands, keywords, requiredSecrets, and conflicts based on their official READMEs. These are search/data-oriented plugins each with their own independent GitHub repository.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified metadata, verificationStatus set to "verified" for confirmed plugins.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns the executor needs, same as Phase 2 plans -->

From lib/plugins.ts — the update pattern:
- CORE_PLUGINS record: update `desc`, `longDesc`, `features`, `keywords`, `install`, `url`, `githubRepo`, `conflicts` fields
- PLUGIN_FIELD_OVERRIDES record: update `verificationStatus`, `officialStatus`, `difficulty`, `requiredSecrets`, `bestFor`, `avoidFor`, `installMode`, `prerequisites`
- Default verificationStatus is "partial" — update to "verified" after confirming against README

From lib/i18n/plugins-en.ts — the sync pattern:
- pluginDescEn: update `desc` and `longDesc` English translations to match updated Korean text
- reasonsEn: update recommendation reason text if features/description changed significantly

Target GitHub repos:
- notion: https://github.com/makenotion/notion-mcp-server
- firecrawl: https://github.com/mendableai/firecrawl-mcp-server
- exa: https://github.com/exa-labs/exa-mcp-server
- tavily: https://github.com/tavily-ai/tavily-mcp

Current PLUGIN_FIELD_OVERRIDES state:
- notion: officialStatus "official", installMode "external-setup", requiredSecrets ["Notion integration token"]
- firecrawl: officialStatus "official", installMode "external-setup", requiredSecrets ["Firecrawl API key"]
- exa: officialStatus "official", installMode "external-setup", requiredSecrets ["Exa API key"]
- tavily: officialStatus "official", installMode "external-setup", requiredSecrets ["Tavily API key"]
- All 4 have verificationStatus defaulting to "partial" (no explicit override)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for notion, firecrawl, exa, tavily</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 4 plugins (notion, firecrawl, exa, tavily), perform the following verification steps against their independent GitHub repos:

**Step 0: Fetch source data**
Use WebFetch or GitHub raw content to retrieve README.md from each repo:
- https://github.com/makenotion/notion-mcp-server (or raw README)
- https://github.com/mendableai/firecrawl-mcp-server (or raw README)
- https://github.com/exa-labs/exa-mcp-server (or raw README)
- https://github.com/tavily-ai/tavily-mcp (or raw README)

Also check package.json if available to confirm the correct npm package name for install commands.

**Step 1: VERIFY-01 — Confirm repo existence and activity**
Verify each GitHub repo exists and is active (not archived, not empty). Log confirmation including most recent commit activity if visible.

**Step 2: VERIFY-02 — Description accuracy**
Compare current `desc` and `longDesc` (Korean) against README content. Update if:
- README reveals features/capabilities not reflected in current descriptions
- Current description overstates or understates actual functionality
- Keep descriptions concise and in the same style as Phase 2 updates

**Step 3: VERIFY-03 — Features accuracy**
Compare current `features` array against actual tools/capabilities in README:
- notion: currently ["페이지 CRUD", "데이터베이스 쿼리", "블록 단위 편집", "댓글/코멘트 관리"] — verify against README's Tools section
- firecrawl: currently ["동적 페이지 크롤링", "데이터 추출", "파이프라인 구축", "스케줄링"] — verify against actual documented tools. NOTE: current install uses `/plugin marketplace` format which looks wrong for an MCP server
- exa: currently ["시맨틱 검색", "자연어 쿼리", "콘텐츠 하이라이트", "카테고리 필터"] — verify against README
- tavily: currently ["검색 + 자동 요약", "소스 신뢰도 평가", "심층 검색 모드", "AI 에이전트 최적화"] — verify against README

**Step 4: VERIFY-04 — Keywords accuracy**
Check if `keywords` reflect actual use cases from READMEs. Add missing relevant keywords, remove misleading ones.

**Step 5: VERIFY-05 — Install command accuracy**
Verify install commands match each README's Claude Code install instructions:
- notion: currently `claude mcp add notion -- npx -y @notionhq/notion-mcp-server` — verify package name from README/package.json
- firecrawl: currently uses `/plugin marketplace add firecrawl` — this looks WRONG for an MCP server. Check README for correct `claude mcp add` format
- exa: currently `claude mcp add exa -- npx -y exa-mcp-server` — verify package name
- tavily: currently `claude mcp add tavily -- npx -y tavily-mcp@latest` — verify package name

**Step 6: VERIFY-06 — Conflicts accuracy**
- notion: `conflicts: []` — confirm no conflicts
- firecrawl: `conflicts: []` — confirm no conflicts
- exa: `conflicts: []` — consider whether exa should conflict with brave-search/tavily/perplexity (all are search tools, but semantic vs web search are different use cases — likely no conflict)
- tavily: `conflicts: ["brave-search"]` — verify this design decision is still valid (both are web search APIs — recommender should avoid suggesting both)

**Step 7: Update PLUGIN_FIELD_OVERRIDES**
- Set `verificationStatus: "verified"` for each confirmed plugin
- Verify `requiredSecrets` use the correct environment variable names from READMEs (e.g., "Notion integration token" should ideally include the actual env var name like NOTION_API_KEY or NOTION_TOKEN)
- Verify `difficulty` and `installMode` are reasonable
- Add `bestFor`/`avoidFor` if not already present

**Step 8: Sync English translations**
Update `pluginDescEn` entries for all 4 plugins to match updated Korean descriptions.
Update `reasonsEn` entries if recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs — they are keys throughout the codebase
- Do NOT modify plugins from Phase 1 or Phase 2 — out of scope
- Do NOT add or remove plugins — only verify/update existing metadata
- Do NOT use `any` type — per project conventions
- If a repo is unavailable or dead, set verificationStatus to "unverified" with a code comment (same pattern as ralph in Phase 1)
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- notion, firecrawl, exa, tavily metadata verified against their GitHub READMEs
- verificationStatus updated to "verified" (or "unverified" with documented reason)
- requiredSecrets include correct env var names from READMEs
- firecrawl install command fixed to use `claude mcp add` format (if currently wrong)
- English translations synced in plugins-en.ts
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- All 4 plugins have verificationStatus updated in PLUGIN_FIELD_OVERRIDES
- Install commands follow `claude mcp add {name} -- npx -y {package}` pattern
- requiredSecrets include actual env var names from READMEs
- firecrawl install no longer uses `/plugin marketplace` format
- tavily conflicts: ["brave-search"] verified as valid design decision
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions
</verification>

<success_criteria>
- notion, firecrawl, exa, tavily metadata matches their official GitHub READMEs
- Install commands use correct package names verified from READMEs/package.json
- requiredSecrets correctly lists env var names for all 4 plugins
- verificationStatus updated to "verified" for all 4 (or "unverified" with documented reason)
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-official-plugins/03-01-SUMMARY.md`
</output>
