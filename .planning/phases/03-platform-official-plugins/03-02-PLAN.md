---
phase: 03-platform-official-plugins
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "perplexity, sentry, figma 3개 플러그인의 공식 GitHub repo가 존재하고 활성 상태임이 확인되었다"
    - "3개 플러그인의 install 명령어가 각 공식 README의 Claude Code 설치 방법과 일치한다"
    - "3개 플러그인의 features, keywords가 실제 README에 문서화된 기능을 정확히 반영한다"
    - "figma의 difficulty: advanced와 prerequisites가 실제 요구사항을 반영한다"
    - "sentry의 requiredSecrets가 정확한 환경변수명을 포함한다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for perplexity, sentry, figma"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for perplexity, sentry, figma"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "perplexity|sentry|figma"
---

<objective>
Verify and update metadata for perplexity, sentry, and figma plugins against their official GitHub repos.

Purpose: Ensure these 3 standalone official plugins have accurate descriptions, features, install commands, keywords, requiredSecrets, and prerequisites based on their official READMEs. Special attention to figma's advanced difficulty and prerequisites validation.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified metadata, verificationStatus set to "verified" for confirmed plugins.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-official-plugins/03-01-SUMMARY.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns, same as Plan 01 -->

From lib/plugins.ts — the update pattern:
- CORE_PLUGINS record: update desc, longDesc, features, keywords, install, url, githubRepo, conflicts
- PLUGIN_FIELD_OVERRIDES record: update verificationStatus, officialStatus, difficulty, requiredSecrets, bestFor, avoidFor, installMode, prerequisites
- Default verificationStatus is "partial" — update to "verified" after confirming against README

From lib/i18n/plugins-en.ts — the sync pattern:
- pluginDescEn: update desc and longDesc English translations
- reasonsEn: update recommendation reason text if changed

Target GitHub repos:
- perplexity: https://github.com/ppl-ai/modelcontextprotocol
- sentry: https://github.com/getsentry/sentry-mcp
- figma: https://github.com/figma/figma-mcp

Current PLUGIN_FIELD_OVERRIDES state:
- perplexity: officialStatus "official", installMode "external-setup", requiredSecrets ["Perplexity API key"]
- sentry: officialStatus "official", installMode "external-setup", requiredSecrets ["Sentry auth token"]
- figma: officialStatus "official", difficulty "advanced", installMode "external-setup", verificationStatus "partial", requiredSecrets ["Figma API key"]

Current install commands:
- perplexity: `claude mcp add perplexity -- npx -y perplexity-mcp@latest`
- sentry: `claude mcp add sentry -- npx -y @sentry/mcp-server`
- figma: `claude mcp add figma -- npx -y figma-developer-mcp --figma-api-key=YOUR_KEY`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for perplexity, sentry, figma</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 3 plugins (perplexity, sentry, figma), perform the following verification steps against their independent GitHub repos:

**Step 0: Fetch source data**
Use WebFetch or GitHub raw content to retrieve README.md from each repo:
- https://github.com/ppl-ai/modelcontextprotocol (or raw README)
- https://github.com/getsentry/sentry-mcp (or raw README)
- https://github.com/figma/figma-mcp (or raw README)

Also check package.json if available to confirm correct npm package names.

**Step 1: VERIFY-01 — Confirm repo existence and activity**
Verify each GitHub repo exists and is active. Log confirmation.

**Step 2: VERIFY-02 — Description accuracy**
Compare current desc and longDesc (Korean) against README content. Update if README reveals features/capabilities not reflected, or if current description overstates functionality.

**Step 3: VERIFY-03 — Features accuracy**
Compare current features array against actual tools in README:
- perplexity: currently ["소스 기반 답변", "실시간 정보 반영", "기술 비교 분석", "인용 링크 제공"] — verify against README's actual tool/capability list
- sentry: currently ["실시간 에러 조회", "스택 트레이스 분석", "에러 패턴 분류", "자동 수정 제안"] — verify against README's Tools section. "자동 수정 제안" may be overstated
- figma: currently ["디자인 데이터 읽기", "디자인 → 코드 변환", "스타일 토큰 추출", "컴포넌트 구조 분석"] — verify against README's actual tools/capabilities

**Step 4: VERIFY-04 — Keywords accuracy**
Check keywords reflect actual use cases from READMEs. Add missing, remove misleading.

**Step 5: VERIFY-05 — Install command accuracy**
Verify install commands match README instructions:
- perplexity: `claude mcp add perplexity -- npx -y perplexity-mcp@latest` — verify package name and whether @latest is needed
- sentry: `claude mcp add sentry -- npx -y @sentry/mcp-server` — verify correct scoped package name
- figma: `claude mcp add figma -- npx -y figma-developer-mcp --figma-api-key=YOUR_KEY` — verify package name and whether API key is passed as CLI arg or env var

**Step 6: VERIFY-06 — Conflicts accuracy**
- All 3 currently have `conflicts: []` — confirm no conflicts needed

**Step 7: Update PLUGIN_FIELD_OVERRIDES**
- Set `verificationStatus: "verified"` for each confirmed plugin
- Verify requiredSecrets with correct env var names from READMEs:
  - perplexity: what env var? PERPLEXITY_API_KEY?
  - sentry: what env var? SENTRY_AUTH_TOKEN?
  - figma: what env var/arg? FIGMA_API_KEY or --figma-api-key?
- Verify figma difficulty "advanced" — check if README requires complex setup (API key + additional config)
- Add prerequisites for figma if README specifies requirements beyond the API key
- Add bestFor/avoidFor where not already present

**Step 8: Sync English translations**
Update pluginDescEn entries for all 3 plugins to match updated Korean descriptions.
Update reasonsEn entries if recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs
- Do NOT modify plugins from Plan 01 (notion, firecrawl, exa, tavily) or earlier phases
- Do NOT add or remove plugins
- Do NOT use `any` type
- If a repo is unavailable, set verificationStatus to "unverified" with a code comment
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- perplexity, sentry, figma metadata verified against their GitHub READMEs
- verificationStatus updated to "verified" (or "unverified" with documented reason)
- figma difficulty and prerequisites validated against README requirements
- requiredSecrets include correct env var names from READMEs
- English translations synced in plugins-en.ts
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- All 3 plugins have verificationStatus updated in PLUGIN_FIELD_OVERRIDES
- Install commands match README-documented installation methods
- requiredSecrets include actual env var names from READMEs
- figma difficulty "advanced" is justified by actual setup complexity
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions
</verification>

<success_criteria>
- perplexity, sentry, figma metadata matches their official GitHub READMEs
- Install commands use correct package names verified from READMEs
- requiredSecrets correctly lists env var names for all 3 plugins
- figma prerequisites reflect actual requirements from README
- verificationStatus updated to "verified" for all 3 (or "unverified" with documented reason)
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-official-plugins/03-02-SUMMARY.md`
</output>
