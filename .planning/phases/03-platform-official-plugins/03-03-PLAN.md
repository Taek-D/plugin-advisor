---
phase: 03-platform-official-plugins
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "docker, cloudflare, vercel 3개 플러그인의 공식 GitHub repo(또는 공식 문서 URL)가 확인되었다"
    - "3개 플러그인의 install 명령어가 각 공식 README의 Claude Code 설치 방법과 일치한다"
    - "3개 플러그인의 features, keywords가 실제 README에 문서화된 기능을 정확히 반영한다"
    - "docker의 difficulty: advanced와 prerequisites(Docker Desktop/Engine)가 실제 요구사항을 반영한다"
    - "cloudflare의 difficulty: advanced와 requiredSecrets가 실제 설정에 필요한 항목과 일치한다"
    - "vercel의 githubRepo가 확인되거나 null로 유지된 이유가 문서화되었다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for docker, cloudflare, vercel"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for docker, cloudflare, vercel"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "docker|cloudflare|vercel"
---

<objective>
Verify and update metadata for docker, cloudflare, and vercel plugins against their official GitHub repos or documentation.

Purpose: Ensure these 3 DevOps/platform plugins have accurate descriptions, features, install commands, keywords, requiredSecrets, and prerequisites. Special attention to docker and cloudflare's advanced difficulty validation, and vercel's missing githubRepo field.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified metadata, verificationStatus set to "verified" for confirmed plugins.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-official-plugins/03-02-SUMMARY.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns, same as Plans 01-02 -->

From lib/plugins.ts — the update pattern:
- CORE_PLUGINS record: update desc, longDesc, features, keywords, install, url, githubRepo, conflicts
- PLUGIN_FIELD_OVERRIDES record: update verificationStatus, officialStatus, difficulty, requiredSecrets, bestFor, avoidFor, installMode, prerequisites
- Default verificationStatus is "partial" — update to "verified" after confirming against README

From lib/i18n/plugins-en.ts — the sync pattern:
- pluginDescEn: update desc and longDesc English translations
- reasonsEn: update recommendation reason text if changed

Target GitHub repos:
- docker: https://github.com/docker/docker-mcp
- cloudflare: https://github.com/cloudflare/mcp-server-cloudflare
- vercel: githubRepo is currently null, url is https://vercel.com/docs/mcp — search for actual repo (likely vercel/vercel-mcp or similar)

Current PLUGIN_FIELD_OVERRIDES state:
- docker: officialStatus "official", difficulty "advanced", installMode "external-setup", prerequisites ["Docker Desktop 또는 Docker Engine"]
- cloudflare: officialStatus "official", difficulty "advanced", installMode "external-setup", requiredSecrets ["Cloudflare API token"]
- vercel: officialStatus "official", installMode "external-setup", requiredSecrets ["Vercel account access"]

Current install commands:
- docker: `claude mcp add docker -- npx -y @docker/mcp-server`
- cloudflare: `claude mcp add cloudflare -- npx -y @cloudflare/mcp-server-cloudflare`
- vercel: `claude mcp add vercel -- npx -y @vercel/mcp`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for docker, cloudflare, vercel</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 3 plugins (docker, cloudflare, vercel), perform the following verification steps:

**Step 0: Fetch source data**
Use WebFetch or GitHub raw content to retrieve README.md from each repo:
- https://github.com/docker/docker-mcp (or raw README)
- https://github.com/cloudflare/mcp-server-cloudflare (or raw README)
- For vercel: first try to find the actual GitHub repo — search for `vercel/vercel-mcp`, `vercel/mcp`, or check https://vercel.com/docs/mcp for the repo link. If no public repo exists, use the docs page as the source of truth.

**Step 1: VERIFY-01 — Confirm repo existence and activity**
- docker: verify docker/docker-mcp exists and is active
- cloudflare: verify cloudflare/mcp-server-cloudflare exists and is active
- vercel: find the actual GitHub repo URL. If found, update `githubRepo` from null. If no public repo, document why githubRepo remains null and use the npm package + docs page for verification.

**Step 2: VERIFY-02 — Description accuracy**
Compare current desc and longDesc (Korean) against README/docs content. Update if description misrepresents actual functionality.

**Step 3: VERIFY-03 — Features accuracy**
Compare current features array against actual tools in README:
- docker: currently ["컨테이너 관리", "이미지 빌드", "로그 실시간 조회", "Docker Compose 지원"] — verify against README's actual tools list
- cloudflare: currently ["Workers 배포", "KV/R2/D1 관리", "엣지 컴퓨팅", "글로벌 CDN 설정"] — verify against README. "글로벌 CDN 설정" may be overstated
- vercel: currently ["원클릭 배포", "환경 변수 관리", "프리뷰 배포", "배포 로그 조회"] — verify against README/docs

**Step 4: VERIFY-04 — Keywords accuracy**
Check keywords reflect actual use cases. Add missing, remove misleading.

**Step 5: VERIFY-05 — Install command accuracy**
Verify install commands match README/docs:
- docker: `claude mcp add docker -- npx -y @docker/mcp-server` — verify package name
- cloudflare: `claude mcp add cloudflare -- npx -y @cloudflare/mcp-server-cloudflare` — verify package name
- vercel: `claude mcp add vercel -- npx -y @vercel/mcp` — verify package name

**Step 6: VERIFY-06 — Conflicts accuracy**
All 3 currently have `conflicts: []` — confirm no conflicts needed.

**Step 7: Update PLUGIN_FIELD_OVERRIDES**
- Set `verificationStatus: "verified"` for each confirmed plugin
- docker: verify prerequisites ["Docker Desktop 또는 Docker Engine"] — does README require Docker to be installed? Verify difficulty "advanced" is justified
- cloudflare: verify requiredSecrets — what is the actual env var name? CLOUDFLARE_API_TOKEN? Verify difficulty "advanced" — does README require complex Cloudflare account setup?
- vercel: verify requiredSecrets ["Vercel account access"] — what is the actual env var? VERCEL_TOKEN? Update to specific env var name
- Add bestFor/avoidFor where not already present

**Step 8: Sync English translations**
Update pluginDescEn entries for all 3 plugins to match updated Korean descriptions.
Update reasonsEn entries if recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs
- Do NOT modify plugins from Plans 01-02 or earlier phases
- Do NOT add or remove plugins
- Do NOT use `any` type
- If a repo is unavailable, set verificationStatus to "unverified" with a code comment
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- docker, cloudflare, vercel metadata verified against their GitHub READMEs or official docs
- verificationStatus updated to "verified" (or "unverified" with documented reason)
- docker prerequisites and difficulty validated against README
- cloudflare difficulty and requiredSecrets validated against README
- vercel githubRepo updated if repo found, or null retention documented
- requiredSecrets include correct env var names
- English translations synced in plugins-en.ts
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- All 3 plugins have verificationStatus updated in PLUGIN_FIELD_OVERRIDES
- Install commands match README/docs-documented installation methods
- docker prerequisites reflect actual Docker runtime requirements
- cloudflare and docker difficulty "advanced" is justified by actual setup complexity
- vercel githubRepo is either filled or null-retention is documented
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions
</verification>

<success_criteria>
- docker, cloudflare, vercel metadata matches their official GitHub READMEs or docs
- Install commands use correct package names verified from READMEs
- requiredSecrets correctly lists env var names for all 3 plugins
- docker and cloudflare prerequisites reflect actual requirements
- verificationStatus updated to "verified" for all 3 (or "unverified" with documented reason)
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-official-plugins/03-03-SUMMARY.md`
</output>
