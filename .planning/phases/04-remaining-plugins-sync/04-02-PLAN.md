---
phase: 04-remaining-plugins-sync
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [UPDATE-01, UPDATE-02]

must_haves:
  truths:
    - "모든 26개 검증 대상 플러그인의 verificationStatus가 'partial'이 아닌 근거 있는 값('verified' 또는 'unverified')으로 설정되었다"
    - "Tier 1 신규 6개 플러그인(aws, atlassian, browserbase, stripe, neon, desktop-commander)과 supabase의 verificationStatus가 명시적으로 평가되었다"
    - "lib/i18n/plugins-en.ts의 모든 pluginDescEn/reasonsEn 항목이 lib/plugins.ts의 현재 desc/longDesc/features와 동기화되었다"
    - "pluginDescEn과 reasonsEn에 lib/plugins.ts CORE_PLUGINS의 모든 플러그인 ID가 빠짐없이 존재한다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "All 26 plugins with explicit verificationStatus in PLUGIN_FIELD_OVERRIDES"
      contains: "verificationStatus"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Complete English translations synced with current Korean metadata"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "Every plugin ID in CORE_PLUGINS must have matching entries in pluginDescEn and reasonsEn"
      pattern: "pluginDescEn|reasonsEn"
---

<objective>
Audit all 26 verification-target plugins for verificationStatus completeness and sync all English translations.

Purpose: After Plan 01 completes the final 3 per-plugin verifications, this plan ensures no plugin is left with a default "partial" status that lacks evidence, and that all English translations are current. This is the cross-cutting sync that closes out the entire v1.0 milestone.
Output: Every plugin has an explicit verificationStatus in PLUGIN_FIELD_OVERRIDES. English translations in plugins-en.ts are fully synchronized with current plugins.ts content.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remaining-plugins-sync/04-01-SUMMARY.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key understanding for the executor -->

From lib/plugins.ts -- verificationStatus audit targets:

Plugins that already have explicit verificationStatus: "verified" in PLUGIN_FIELD_OVERRIDES (do NOT change):
- omc, superpowers, bkit-starter, bkit, context7, repomix, playwright, puppeteer, security, firecrawl, exa, tavily, perplexity, brave-search, aws, stripe, notion, figma, cloudflare, docker, vercel, github, slack, sentry, postgres, filesystem, memory, git, sequential-thinking, taskmaster, gsd, fireauto, agency-agents

Plugins with verificationStatus: "unverified" (do NOT change unless new evidence):
- ralph (repo 404), uiux (TBD from Plan 01)

Plugins with verificationStatus: "partial" or NO explicit override that need audit:
- atlassian: explicit "partial" -- Tier 1 plugin added recently, not yet verified against repo
- browserbase: explicit "partial" -- Tier 1 plugin added recently, not yet verified against repo
- neon: explicit "partial" -- Tier 1 plugin added recently, not yet verified against repo
- desktop-commander: explicit "partial" -- Tier 1 plugin added recently, not yet verified against repo
- supabase: NO verificationStatus override at all -- defaults to "partial"
- todoist: TBD from Plan 01 (should be resolved)
- linear: TBD from Plan 01 (should be resolved)

From lib/i18n/plugins-en.ts -- sync targets:
- pluginDescEn: Record<string, { desc: string; longDesc: string }>
- reasonsEn: Record<string, string>
- Both must have entries for EVERY plugin ID in CORE_PLUGINS
- Translations must reflect current Korean desc/longDesc content after all Phase 1-4 updates

Tier 1 plugins (aws, atlassian, browserbase, stripe, neon, desktop-commander) were added in commit ea451f6.
These may or may not have English translations yet -- check and add if missing.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit verificationStatus for remaining partial plugins</name>
  <files>lib/plugins.ts</files>
  <action>
Perform a systematic audit of every plugin that still has verificationStatus "partial" (explicit or default). The goal is NOT to do full README verification for each (that would be a separate milestone), but to set an honest, evidence-based status.

**For each "partial" plugin, apply this decision matrix:**

1. If the plugin was verified in Phase 1-4 (todoist, linear from Plan 01) -- confirm Plan 01 already set the status. Skip.

2. If the plugin is a Tier 1 addition (aws, atlassian, browserbase, stripe, neon, desktop-commander) that was added in commit ea451f6 without full verification:
   - These were NOT part of the v1.0 verification scope (REQUIREMENTS.md scope is 26 plugins from Phase 1-3 + Phase 4's todoist/linear/uiux)
   - Set verificationStatus to "partial" EXPLICITLY in PLUGIN_FIELD_OVERRIDES (not relying on default) with a code comment: `// Tier 1 addition — not yet verified against repo README. Queued for v2 verification.`
   - This makes the "partial" status intentional and documented rather than a forgotten default

3. If the plugin is supabase (no verificationStatus override at all):
   - supabase githubRepo is null, officialStatus is "official"
   - Add explicit `verificationStatus: "partial"` to its PLUGIN_FIELD_OVERRIDES entry with comment: `// supabase: official plugin, metadata not yet verified against repo/docs`

**After the audit, verify that EVERY plugin ID in CORE_PLUGINS has an explicit verificationStatus in PLUGIN_FIELD_OVERRIDES.** No plugin should rely on the default "partial" -- either it is explicitly "verified", "unverified", or "partial" with a documented reason.

**Summary of expected changes:**
- atlassian: add comment to existing "partial" entry
- browserbase: add comment to existing "partial" entry
- neon: add comment to existing "partial" entry
- desktop-commander: add comment to existing "partial" entry
- supabase: add `verificationStatus: "partial"` with comment
- Confirm todoist, linear already resolved by Plan 01

**What to avoid and WHY:**
- Do NOT change any "verified" status -- those were confirmed against READMEs in Phase 1-3
- Do NOT do full README verification for Tier 1 plugins -- that is v2 scope per REQUIREMENTS.md
- Do NOT change "unverified" plugins (ralph, uiux) unless Plan 01 found new evidence
- Do NOT remove any plugins or change plugin IDs
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
- Every plugin in CORE_PLUGINS has an explicit verificationStatus in PLUGIN_FIELD_OVERRIDES
- No plugin relies on the default "partial" from DEFAULT_PLUGIN_FIELDS
- Each "partial" status has a code comment explaining why it is partial
- pnpm typecheck and lint pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Full i18n sync -- ensure all plugins have complete English translations</name>
  <files>lib/i18n/plugins-en.ts</files>
  <action>
Perform a complete sync of English translations to ensure every plugin has accurate, up-to-date entries.

**Step 1: Completeness check**
Compare all plugin IDs in CORE_PLUGINS (lib/plugins.ts) against keys in pluginDescEn and reasonsEn (lib/i18n/plugins-en.ts). Identify:
- Plugin IDs present in CORE_PLUGINS but MISSING from pluginDescEn
- Plugin IDs present in CORE_PLUGINS but MISSING from reasonsEn
- Any orphaned keys in pluginDescEn/reasonsEn that do not exist in CORE_PLUGINS

**Step 2: Add missing translations**
For any plugin ID missing from pluginDescEn or reasonsEn:
- Write English desc and longDesc that accurately translate the current Korean text in CORE_PLUGINS
- Write English recommendation reason that matches the Korean reason text
- Tier 1 plugins (aws, atlassian, browserbase, stripe, neon, desktop-commander) may be missing -- check and add

**Step 3: Freshness audit**
For plugins whose Korean desc/longDesc/features were updated in Phase 1-4 (omc, agency-agents, ralph, sequential-thinking, brave-search, puppeteer, filesystem, git, postgres, memory, github, slack, notion, firecrawl, exa, tavily, perplexity, sentry, figma, docker, cloudflare, vercel, todoist, linear, uiux):
- Compare English translations against current Korean text
- If Korean was updated but English was not synced, update the English translation
- Pay special attention to: install method changes reflected in longDesc, feature changes, requiredSecrets mentions

**Step 4: Style consistency**
Ensure all English translations follow the same style:
- desc: 1-2 sentences, concise, action-oriented
- longDesc: 3-5 sentences, includes tool count if applicable, mentions env vars if needed
- reasonsEn: 1-2 sentences explaining why plugin is recommended for the detected use case

**What to avoid and WHY:**
- Do NOT modify lib/plugins.ts in this task -- only lib/i18n/plugins-en.ts
- Do NOT add new plugin IDs to the English file that do not exist in CORE_PLUGINS
- Do NOT use machine-translated text that sounds unnatural -- write natural English
- Do NOT use `any` type
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- Every plugin ID in CORE_PLUGINS has a matching entry in pluginDescEn
- Every plugin ID in CORE_PLUGINS has a matching entry in reasonsEn
- No orphaned keys exist in plugins-en.ts
- English translations accurately reflect current Korean metadata after all Phase 1-4 changes
- Tier 1 plugins have English translations
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- `grep -c "verificationStatus" lib/plugins.ts` shows every plugin has an explicit status in PLUGIN_FIELD_OVERRIDES
- No plugin relies on DEFAULT_PLUGIN_FIELDS for verificationStatus
- pluginDescEn key count matches CORE_PLUGINS key count
- reasonsEn key count matches CORE_PLUGINS key count
- No TypeScript errors, lint errors, or build failures
- English translations are natural and accurate
</verification>

<success_criteria>
- UPDATE-01: All 26+ plugins have explicit verificationStatus in PLUGIN_FIELD_OVERRIDES with documented reasoning
- UPDATE-02: lib/i18n/plugins-en.ts is fully synchronized -- no missing translations, no stale translations
- pnpm typecheck && pnpm lint && pnpm build all pass
- The v1.0 milestone verification work is complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-remaining-plugins-sync/04-02-SUMMARY.md`
</output>
