---
phase: 04-remaining-plugins-sync
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "todoist(abhiz123/todoist-mcp-server) GitHub repo가 존재하고 활성 상태임이 확인되었고, 메타데이터가 README와 일치한다"
    - "linear(jerhadf/linear-mcp-server) GitHub repo가 존재하고 활성 상태임이 확인되었고, 메타데이터가 README와 일치한다"
    - "uiux 플러그인의 실제 repo 존재 여부가 확인되어 verificationStatus가 근거 있는 값으로 설정되었다"
    - "3개 플러그인의 install 명령어가 각 공식 README의 설치 방법과 일치한다"
    - "3개 플러그인의 features, requiredSecrets가 실제 README에 문서화된 기능/환경변수를 정확히 반영한다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for todoist, linear, uiux"
      contains: "verificationStatus"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for todoist, linear, uiux"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "todoist|linear|uiux"
---

<objective>
Verify and update metadata for todoist, linear, and uiux plugins against their GitHub repos (or confirm absence).

Purpose: Complete the per-plugin verification for the final 3 unverified plugins. todoist and linear have known GitHub repos to check. uiux has githubRepo: null and a placeholder URL (yourusername/ui-ux-pro-max) -- need to determine if a real repo exists or confirm it cannot be verified.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified or appropriately flagged metadata for all 3 plugins.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns the executor needs, same as Phase 1-3 plans -->

From lib/plugins.ts -- the update pattern:
- CORE_PLUGINS record: update `desc`, `longDesc`, `features`, `keywords`, `install`, `url`, `githubRepo`, `conflicts` fields
- PLUGIN_FIELD_OVERRIDES record: update `verificationStatus`, `officialStatus`, `difficulty`, `requiredSecrets`, `bestFor`, `avoidFor`, `installMode`, `prerequisites`
- Default verificationStatus is "partial" -- update to "verified" after confirming against README
- For unverifiable plugins, set verificationStatus to "unverified" with a code comment explaining why (same pattern as ralph)

From lib/i18n/plugins-en.ts -- the sync pattern:
- pluginDescEn: update `desc` and `longDesc` English translations to match updated Korean text
- reasonsEn: update recommendation reason text if features/description changed significantly

Current PLUGIN_FIELD_OVERRIDES state for target plugins:
- todoist: `installMode: "external-setup"`, `requiredSecrets: ["Todoist API token"]` -- no verificationStatus override (defaults to "partial")
- linear: `installMode: "external-setup"`, `requiredSecrets: ["Linear API access"]` -- no verificationStatus override (defaults to "partial")
- uiux: `officialStatus: "unknown"`, `verificationStatus: "unverified"`, `difficulty: "advanced"`, `installMode: "manual-required"`, `maintenanceStatus: "unclear"`

Current CORE_PLUGINS state for target plugins:
- todoist: githubRepo "abhiz123/todoist-mcp-server", install `claude mcp add todoist -- npx -y todoist-mcp-server`
- linear: githubRepo "jerhadf/linear-mcp-server", install `claude mcp add linear -- npx -y linear-mcp-server`
- uiux: githubRepo null, url "https://github.com/yourusername/ui-ux-pro-max", install `["/plugin marketplace add ...", "/plugin install ui-ux-pro-max"]`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for todoist, linear, uiux</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 3 plugins, perform the full VERIFY-01 through VERIFY-06 verification against their GitHub repos.

**Plugin 1: todoist (abhiz123/todoist-mcp-server)**

Step 0: Fetch README from https://github.com/abhiz123/todoist-mcp-server (use WebFetch or raw GitHub content). Also check package.json for correct npm package name.

Step 1 (VERIFY-01): Confirm repo exists and is active (not archived, not empty). If 404, set verificationStatus to "unverified" with a comment and skip remaining steps.

Step 2 (VERIFY-02): Compare current desc/longDesc against README. Current Korean desc: "Todoist 연동으로 개발 태스크를 실시간 관리. 할 일 추적의 정석." Update if README reveals different capabilities.

Step 3 (VERIFY-03): Compare features array against actual tools in README. Current: ["태스크 자동 생성", "프로젝트별 분류", "우선순위 관리", "마감일 설정"]. Verify each feature maps to a documented tool/capability.

Step 4 (VERIFY-04): Check keywords reflect actual use cases.

Step 5 (VERIFY-05): Verify install command. Current: `claude mcp add todoist -- npx -y todoist-mcp-server`. Check if package name is correct per README/package.json. Check if env var is needed for the install command (e.g., --env flag for API token).

Step 6 (VERIFY-06): Verify conflicts: [] is correct.

Step 7: Update PLUGIN_FIELD_OVERRIDES -- set verificationStatus to "verified" (or "unverified" with comment if repo is dead). Update requiredSecrets to use exact env var name from README (current: "Todoist API token" -- should be the actual env var like TODOIST_API_TOKEN or TODOIST_API_KEY).

**Plugin 2: linear (jerhadf/linear-mcp-server)**

Repeat the same VERIFY-01 through VERIFY-06 steps for linear:
- Fetch README from https://github.com/jerhadf/linear-mcp-server
- Verify repo existence and activity
- Compare desc/longDesc, features, keywords, install command, conflicts against README
- Current install: `claude mcp add linear -- npx -y linear-mcp-server` -- verify package name
- Current requiredSecrets: ["Linear API access"] -- find exact env var name from README (likely LINEAR_API_KEY or similar)
- Update PLUGIN_FIELD_OVERRIDES with verificationStatus and correct requiredSecrets

**Plugin 3: uiux**

This plugin has githubRepo: null and a placeholder URL (yourusername/ui-ux-pro-max). The goal is to determine if a real repo exists.

Step 1: Search GitHub for "ui-ux-pro-max" or "uiux pro max" MCP server. Try:
- https://github.com/yourusername/ui-ux-pro-max (known placeholder -- will 404)
- Search npm registry for "ui-ux-pro-max" package
- Search GitHub for "ui-ux-pro-max mcp" or similar terms

Step 2: If NO real repo is found:
- Keep verificationStatus: "unverified"
- Keep officialStatus: "unknown"
- Add a code comment: "// uiux: No public GitHub repo found. 'yourusername/ui-ux-pro-max' is a placeholder URL. Verified 2026-03-XX."
- Update install commands to reflect reality (remove fake marketplace URLs)
- Consider whether url field should be set to null or kept as-is with a comment
- Keep maintenanceStatus: "unclear"

Step 3: If a real repo IS found:
- Update githubRepo, url, install with actual values
- Verify all metadata against the real README
- Set verificationStatus appropriately

**Final Step: Sync English translations**
Update pluginDescEn entries for todoist, linear, uiux to match any updated Korean descriptions.
Update reasonsEn entries if recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs -- they are keys throughout the codebase
- Do NOT modify plugins verified in Phase 1, 2, or 3 -- out of scope for this task
- Do NOT add or remove plugins -- only verify/update existing metadata
- Do NOT use `any` type -- per project conventions
- If a repo is unavailable, set verificationStatus to "unverified" with a code comment (same pattern as ralph in Phase 1)
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- todoist metadata verified against abhiz123/todoist-mcp-server README (or marked unverified with reason)
- linear metadata verified against jerhadf/linear-mcp-server README (or marked unverified with reason)
- uiux repo existence investigated; verificationStatus updated with documented evidence
- requiredSecrets include correct env var names from READMEs (not generic descriptions)
- English translations synced in plugins-en.ts for all 3 plugins
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- todoist and linear have verificationStatus updated in PLUGIN_FIELD_OVERRIDES (either "verified" or "unverified" with reason)
- uiux verificationStatus is appropriately set with code comment documenting investigation result
- requiredSecrets use actual env var names (e.g., TODOIST_API_TOKEN not "Todoist API token")
- Install commands match README instructions
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions for all 3 plugins
</verification>

<success_criteria>
- todoist, linear metadata matches their official GitHub READMEs
- uiux repo existence conclusively determined and documented
- All 3 plugins have verificationStatus explicitly set in PLUGIN_FIELD_OVERRIDES
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-remaining-plugins-sync/04-01-SUMMARY.md`
</output>
