---
phase: 18-og-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/og-utils.ts
  - app/opengraph-image.tsx
  - app/twitter-image.tsx
  - app/advisor/opengraph-image.tsx
  - app/plugins/opengraph-image.tsx
  - app/guides/opengraph-image.tsx
  - app/layout.tsx
autonomous: true
requirements: [OGIM-01]

must_haves:
  truths:
    - "사이트 메인 URL 공유 시 Plugin Advisor 브랜딩이 포함된 OG 이미지가 표시된다"
    - "/advisor, /plugins, /guides URL 공유 시 각 페이지에 맞는 OG 이미지가 표시된다"
    - "Twitter에서 공유 시 summary_large_image 카드로 표시된다"
  artifacts:
    - path: "lib/og-utils.ts"
      provides: "Shared OG image utilities (font loader, colors, base styles)"
      exports: ["loadSpaceGrotesk", "OG_COLORS", "OG_SIZE"]
    - path: "app/opengraph-image.tsx"
      provides: "Default brand OG image for root route"
      exports: ["default", "runtime", "alt", "size", "contentType"]
    - path: "app/twitter-image.tsx"
      provides: "Twitter card image for root route"
      exports: ["default", "runtime", "alt", "size", "contentType"]
    - path: "app/advisor/opengraph-image.tsx"
      provides: "Static OG image for /advisor page"
      exports: ["default", "runtime", "alt", "size", "contentType"]
    - path: "app/plugins/opengraph-image.tsx"
      provides: "Static OG image for /plugins catalog page"
      exports: ["default", "runtime", "alt", "size", "contentType"]
    - path: "app/guides/opengraph-image.tsx"
      provides: "Static OG image for /guides listing page"
      exports: ["default", "runtime", "alt", "size", "contentType"]
  key_links:
    - from: "app/opengraph-image.tsx"
      to: "lib/og-utils.ts"
      via: "import shared utilities"
      pattern: "import.*og-utils"
    - from: "app/layout.tsx"
      to: "meta tags"
      via: "openGraph and twitter metadata fields"
      pattern: "openGraph|twitter"
---

<objective>
Create shared OG image utilities and all static OG images for the site's main pages, plus update root layout metadata for social sharing.

Purpose: OGIM-01 requires a branded default OG image and proper social meta tags. This plan also creates static OG images for /advisor, /plugins, /guides so all major pages have custom social cards. The shared utilities (font loader, colors) are created here for reuse by Plan 02's dynamic OG images.

Output: lib/og-utils.ts, 5 opengraph-image.tsx files (root + 3 pages + twitter), updated layout.tsx metadata
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-og-images/18-CONTEXT.md
@.planning/phases/18-og-images/18-RESEARCH.md

@app/layout.tsx
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared OG utilities and root OG/Twitter images</name>
  <files>lib/og-utils.ts, app/opengraph-image.tsx, app/twitter-image.tsx</files>
  <action>
Create `lib/og-utils.ts` with shared utilities for all OG images:
- `OG_SIZE` constant: `{ width: 1200, height: 630 }`
- `OG_COLORS` object with: `background` (dark, e.g. #0f172a), `text` (white/light), `muted` (gray for subtitle/footer), `accent` (brand accent for bars, choose a vibrant color like #6366f1 or similar that fits the site)
- `loadSpaceGrotesk()` async function: fetches Space Grotesk Bold .woff from Google Fonts CDN (`https://fonts.gstatic.com/s/spacegrotesk/v16/V8mDoQDjQSkFtoMM3T6r8E7mPbF4Cw.woff`) and returns ArrayBuffer. Returns the font config object `{ name: "Space Grotesk", data, style: "normal" as const, weight: 700 as const }`.
- `OG_FOOTER_TEXT` constant: `"pluginadvisor.cc"`
- Keep under 60 lines. No default export, only named exports.

Create `app/opengraph-image.tsx` (default brand OG for /):
- `export const runtime = "edge"`
- `export const alt = "Plugin Advisor — Claude Code"`
- `export const size = OG_SIZE`
- `export const contentType = "image/png"`
- Default export: async function returning `new ImageResponse(...)` from `next/og`
- Layout (all inline styles, display: "flex", flexbox only):
  - Dark background (OG_COLORS.background), full width/height
  - Top accent bar: 6px tall, OG_COLORS.accent
  - Center content area (flex-1, centered):
    - "Plugin Advisor" large text (~48px, white, Space Grotesk bold)
    - "Claude Code Starter Setup Guide" subtitle (~24px, OG_COLORS.muted)
  - Bottom area: OG_FOOTER_TEXT in small muted text (~16px), then bottom accent bar 6px
- Pass font from loadSpaceGrotesk() to ImageResponse fonts option

Create `app/twitter-image.tsx`:
- Same design as opengraph-image.tsx (import same utilities, render same layout)
- Same exports (runtime, alt, size, contentType)
- Twitter will use this for the twitter:image meta tag

Both files import from `@/lib/og-utils`. Each file should be under 80 lines.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && npx tsc --noEmit lib/og-utils.ts 2>&1 | head -20 && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>lib/og-utils.ts exports shared utilities. app/opengraph-image.tsx and app/twitter-image.tsx render branded OG images. Build succeeds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create static page OG images and update root metadata</name>
  <files>app/advisor/opengraph-image.tsx, app/plugins/opengraph-image.tsx, app/guides/opengraph-image.tsx, app/layout.tsx</files>
  <action>
Create 3 static OG image files, each following the same pattern as the root OG but with page-specific text:

`app/advisor/opengraph-image.tsx`:
- Same structure (dark bg, accent bars, footer)
- Title: "Plugin Advisor" (~40px)
- Subtitle: "Find the right plugins for your project" (~22px, muted)
- alt: "Plugin Advisor — Find Your Plugins"

`app/plugins/opengraph-image.tsx`:
- Title: "Plugin Catalog" (~40px)
- Subtitle: "Browse all verified Claude Code plugins and MCP servers" (~22px, muted)
- alt: "Plugin Advisor — Plugin Catalog"

`app/guides/opengraph-image.tsx`:
- Title: "Starter Guides" (~40px)
- Subtitle: "Step-by-step guides for your Claude Code setup" (~22px, muted)
- alt: "Plugin Advisor — Starter Guides"

All 3 files: import from `@/lib/og-utils`, export runtime/alt/size/contentType, use OG_COLORS.accent for bars. Each under 80 lines.

Update `app/layout.tsx` metadata export to add openGraph and twitter fields:
- Add to existing `metadata` object (do NOT change title or description):
  ```
  openGraph: {
    type: "website",
    locale: "ko_KR",
    url: "https://pluginadvisor.cc",
    siteName: "Plugin Advisor",
    title: "Plugin Advisor — Claude Code",
    description: "Claude Code 첫 세팅을 덜 헤매게 도와주는 검증된 스타터 스택 설치 어드바이저.",
  },
  twitter: {
    card: "summary_large_image",
    title: "Plugin Advisor — Claude Code",
    description: "Claude Code 첫 세팅을 덜 헤매게 도와주는 검증된 스타터 스택 설치 어드바이저.",
  },
  ```
- Do NOT add `images` to openGraph or twitter — the convention files (opengraph-image.tsx, twitter-image.tsx) auto-inject image URLs
- Do NOT modify any other part of layout.tsx (keep UmamiScript, I18nProvider, Nav, footer, fonts, etc.)
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>3 static OG images render page-specific content. Root layout metadata includes openGraph and twitter fields. `pnpm build` succeeds. All 5 opengraph-image routes are generated at build time.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors — all opengraph-image.tsx files compile
2. `pnpm typecheck` passes
3. After `pnpm dev`, visiting `http://localhost:3000/opengraph-image` returns a PNG image
4. Page source of `/` contains `<meta property="og:image">` and `<meta name="twitter:card" content="summary_large_image">`
</verification>

<success_criteria>
- lib/og-utils.ts exists with shared font loader, colors, and size constants
- 5 opengraph-image.tsx files exist (root, advisor, plugins, guides) + 1 twitter-image.tsx
- Root layout.tsx has openGraph and twitter metadata
- `pnpm build` succeeds with all OG image routes generated
- All OG images use dark background, accent bars, Space Grotesk font, and pluginadvisor.cc footer
</success_criteria>

<output>
After completion, create `.planning/phases/18-og-images/18-01-SUMMARY.md`
</output>
