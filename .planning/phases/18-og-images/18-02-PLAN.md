---
phase: 18-og-images
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - app/plugins/[id]/opengraph-image.tsx
  - app/guides/[slug]/opengraph-image.tsx
autonomous: true
requirements: [OGIM-02, OGIM-03]

must_haves:
  truths:
    - "/plugins/[id] URL 공유 시 해당 플러그인의 이름, 카테고리, 태그가 포함된 동적 OG 이미지가 생성된다"
    - "/guides/[slug] URL 공유 시 해당 가이드의 제목과 설명이 포함된 동적 OG 이미지가 생성된다"
    - "빌드 시 모든 플러그인/가이드의 OG 이미지가 정적으로 사전 생성된다"
  artifacts:
    - path: "app/plugins/[id]/opengraph-image.tsx"
      provides: "Dynamic OG image for each plugin detail page"
      exports: ["default", "runtime", "alt", "size", "contentType", "generateStaticParams"]
    - path: "app/guides/[slug]/opengraph-image.tsx"
      provides: "Dynamic OG image for each guide detail page"
      exports: ["default", "runtime", "alt", "size", "contentType", "generateStaticParams"]
  key_links:
    - from: "app/plugins/[id]/opengraph-image.tsx"
      to: "lib/plugins.ts"
      via: "PLUGINS import for plugin data"
      pattern: "import.*PLUGINS.*plugins"
    - from: "app/plugins/[id]/opengraph-image.tsx"
      to: "lib/og-utils.ts"
      via: "shared OG utilities"
      pattern: "import.*og-utils"
    - from: "app/guides/[slug]/opengraph-image.tsx"
      to: "lib/guides.ts"
      via: "STARTER_GUIDES import for guide data"
      pattern: "import.*STARTER_GUIDES.*guides"
    - from: "app/guides/[slug]/opengraph-image.tsx"
      to: "lib/og-utils.ts"
      via: "shared OG utilities"
      pattern: "import.*og-utils"
---

<objective>
Create dynamic OG images for plugin detail pages (/plugins/[id]) and guide detail pages (/guides/[slug]) with generateStaticParams for build-time pre-rendering.

Purpose: OGIM-02 requires plugin-specific OG images showing name, tag, type badge, and category. OGIM-03 requires guide-specific OG images showing title and summary. Both use the shared utilities from Plan 01.

Output: 2 dynamic opengraph-image.tsx files with generateStaticParams
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-og-images/18-CONTEXT.md
@.planning/phases/18-og-images/18-RESEARCH.md
@.planning/phases/18-og-images/18-01-SUMMARY.md

@lib/og-utils.ts
@lib/types.ts
@lib/plugins.ts
@lib/guides.ts
@app/plugins/[id]/page.tsx
@app/guides/[slug]/page.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 and existing code -->

From lib/og-utils.ts (created in Plan 01):
```typescript
export const OG_SIZE: { width: number; height: number };
export const OG_COLORS: { background: string; text: string; muted: string; accent: string };
export const OG_FOOTER_TEXT: string;
export async function loadSpaceGrotesk(): Promise<{ name: string; data: ArrayBuffer; style: "normal"; weight: 700 }>;
```

From lib/types.ts:
```typescript
export type Plugin = {
  id: string;
  name: string;
  tag: string;
  color: string;       // hex color for accent bar
  category: PluginCategory;
  type: ItemType;       // "mcp" | "plugin"
  // ... other fields
};
```

From lib/plugins.ts:
```typescript
export const PLUGINS: Record<string, Plugin>;
```

From lib/guides.ts:
```typescript
export type StarterGuide = {
  slug: string;
  title: string;        // Korean
  titleEn: string;      // English
  summary: string;      // Korean
  summaryEn: string;    // English
  // ... other fields
};
export const STARTER_GUIDES: StarterGuide[];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create dynamic plugin OG image</name>
  <files>app/plugins/[id]/opengraph-image.tsx</files>
  <action>
Create `app/plugins/[id]/opengraph-image.tsx` for per-plugin dynamic OG images:

- `export const runtime = "edge"`
- `export const alt = "Plugin Advisor"` (will be overridden per-plugin in practice, but alt is static for convention files)
- `export const size = OG_SIZE`
- `export const contentType = "image/png"`

- `export function generateStaticParams()`: return `Object.keys(PLUGINS).map((id) => ({ id }))` — same as page.tsx

- Default export: `async function Image({ params }: { params: { id: string } })`:
  1. Get `const plugin = PLUGINS[params.id]`
  2. If no plugin found, render a fallback with generic "Plugin Advisor" text
  3. Load font via `loadSpaceGrotesk()`
  4. Return `new ImageResponse(...)` with layout:
     - Dark background (OG_COLORS.background), full 1200x630
     - **Top accent bar**: 6px tall, color = `plugin.color` (the plugin's unique color)
     - **Center content** (flex column, centered):
       - Plugin name: large text (~44px, white, bold)
       - Plugin tag: subtitle text (~22px, OG_COLORS.muted), margin-top 12px
       - Badge row (flex row, gap 12px, margin-top 24px):
         - Type badge: rounded pill with text `plugin.type === "mcp" ? "MCP Server" : "Plugin"`, small text (~14px), border 1px solid muted, padding 6px 16px
         - Category badge: rounded pill with `plugin.category` text, same style
     - **Bottom area**: OG_FOOTER_TEXT in small muted text, then bottom accent bar 6px in `plugin.color`
  5. Pass font to ImageResponse options

Badge styling: use inline styles with `borderRadius: 9999`, `border: "1px solid"` + a semi-transparent white/muted color, `padding: "6px 16px"`, `fontSize: 14`, `color: OG_COLORS.muted`.

Keep file under 100 lines. All styles are inline (no Tailwind, no className).
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm build 2>&1 | tail -40</automated>
  </verify>
  <done>app/plugins/[id]/opengraph-image.tsx generates unique OG images per plugin with plugin name, tag, type badge, category badge, and plugin.color accent bars. Build succeeds and pre-renders OG images for all 33+ plugins.</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic guide OG image</name>
  <files>app/guides/[slug]/opengraph-image.tsx</files>
  <action>
Create `app/guides/[slug]/opengraph-image.tsx` for per-guide dynamic OG images:

- `export const runtime = "edge"`
- `export const alt = "Plugin Advisor — Guide"`
- `export const size = OG_SIZE`
- `export const contentType = "image/png"`

- `export function generateStaticParams()`: return `STARTER_GUIDES.map((guide) => ({ slug: guide.slug }))` — same as page.tsx

- Default export: `async function Image({ params }: { params: { slug: string } })`:
  1. Get `const guide = STARTER_GUIDES.find((g) => g.slug === params.slug)`
  2. If no guide found, render fallback with generic "Starter Guides" text
  3. Load font via `loadSpaceGrotesk()`
  4. Return `new ImageResponse(...)` with layout:
     - Dark background (OG_COLORS.background), full 1200x630
     - **Top accent bar**: 6px tall, OG_COLORS.accent (fixed brand color, not per-guide)
     - **Center content** (flex column, centered):
       - Guide title: use `guide.titleEn` (English field to avoid Korean font issues), large text (~40px, white, bold)
       - Guide summary: use `guide.summaryEn` truncated to 120 chars + "..." if longer, subtitle text (~20px, OG_COLORS.muted), margin-top 16px, max-width 900px for wrapping
     - **Bottom area**: OG_FOOTER_TEXT in small muted text, then bottom accent bar 6px in OG_COLORS.accent
  5. Pass font to ImageResponse options

Use English fields (titleEn, summaryEn) per Claude's discretion — avoids Korean font rendering issues in Edge Runtime while keeping text readable. System font fallback for Korean would look inconsistent.

Keep file under 90 lines. All styles are inline.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm build 2>&1 | tail -40</automated>
  </verify>
  <done>app/guides/[slug]/opengraph-image.tsx generates unique OG images per guide with English title and summary, brand accent bars. Build succeeds and pre-renders OG images for all guides.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes — all dynamic OG images pre-rendered for every plugin and guide
2. `pnpm typecheck` passes
3. After `pnpm dev`, visiting `http://localhost:3000/plugins/context7/opengraph-image` returns a PNG with Context7 branding
4. After `pnpm dev`, visiting `http://localhost:3000/guides/claude-code-first-setup-checklist/opengraph-image` returns a PNG with guide title
5. Each plugin OG uses the plugin's unique color for accent bars
6. Each guide OG uses the fixed brand accent color
</verification>

<success_criteria>
- app/plugins/[id]/opengraph-image.tsx exists with generateStaticParams covering all plugins
- app/guides/[slug]/opengraph-image.tsx exists with generateStaticParams covering all guides
- Plugin OG images show: plugin name, tag, type badge (MCP Server/Plugin), category badge, plugin.color accent
- Guide OG images show: English title, English summary (truncated), brand accent
- `pnpm build` succeeds with all dynamic OG images generated
</success_criteria>

<output>
After completion, create `.planning/phases/18-og-images/18-02-SUMMARY.md`
</output>
