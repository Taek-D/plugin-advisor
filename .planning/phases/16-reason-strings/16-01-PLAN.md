---
phase: 16-reason-strings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plugin-reasons.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements:
  - RSN-01
  - RSN-02

must_haves:
  truths:
    - "신규 9개 항목 추천 시 /advisor 카드에 tailored Korean reason이 표시된다 (plugin.desc 대신)"
    - "lib/i18n/plugins-en.ts에 orphaned reasonsEn export가 없다"
    - "pnpm typecheck, lint, build, test가 모두 통과한다"
  artifacts:
    - path: "lib/plugin-reasons.ts"
      provides: "9개 신규 플러그인 Korean reason 문자열"
      contains: "fetch:"
    - path: "lib/i18n/plugins-en.ts"
      provides: "pluginDescEn export만 남은 정리된 파일"
  key_links:
    - from: "lib/plugin-reasons.ts"
      to: "lib/recommend.ts"
      via: "REASONS[plugin.id] || plugin.desc (line 433)"
      pattern: "REASONS\\[plugin\\.id\\]"
---

<objective>
신규 9개 플러그인(fetch, time, markitdown, magic-mcp, n8n-mcp, shadcn-mcp, claude-mem, superclaude, frontend-design)의 tailored Korean reason 문자열을 REASONS 객체에 추가하고, orphaned reasonsEn export를 삭제한다.

Purpose: /advisor 추천 카드에서 신규 항목이 generic desc 대신 맥락에 맞는 추천 이유를 보여주도록 하고, dead export를 제거하여 코드 청결성을 유지한다.
Output: 수정된 lib/plugin-reasons.ts, 정리된 lib/i18n/plugins-en.ts
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reason-strings/16-RESEARCH.md

@lib/plugin-reasons.ts
@lib/i18n/plugins-en.ts
@lib/recommend.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 Korean reason strings to REASONS object (RSN-01)</name>
  <files>lib/plugin-reasons.ts</files>
  <action>
Add 9 new entries to the REASONS object in lib/plugin-reasons.ts, immediately after the `cloudflare` entry (last current entry, line 87). Follow the established pattern exactly: Korean, polite `요.` ending, 1-2 sentences, signal + benefit structure, 40-120 chars total. Keys with hyphens MUST use quoted string keys.

Add these exact entries:

```typescript
// MCP Servers (Phase 13)
fetch:
  "URL 콘텐츠를 가져와야 하는 작업이에요. API 응답이나 웹 페이지를 마크다운으로 변환해 Claude가 바로 읽을 수 있어요.",
time:
  "시간대 변환이나 현재 시각 조회가 필요한 프로젝트예요. 글로벌 팀 일정 조율과 로그 분석에 바로 활용할 수 있어요.",
markitdown:
  "PDF, Word, Excel 등 다양한 문서 파일을 분석해야 해요. 마크다운으로 변환해서 Claude가 문서 내용을 직접 이해할 수 있어요.",
"magic-mcp":
  "AI로 UI 컴포넌트를 빠르게 생성해야 하는 프로젝트예요. 자연어 명령으로 21st.dev 라이브러리 기반 프로덕션 품질 컴포넌트를 즉시 만들 수 있어요.",
"n8n-mcp":
  "워크플로 자동화나 n8n 연동이 필요한 프로젝트예요. n8n 인스턴스 없이도 7개 핵심 도구로 워크플로 설계와 검증을 시작할 수 있어요.",
"shadcn-mcp":
  "shadcn/ui 컴포넌트를 자주 쓰는 프로젝트예요. 공식 레지스트리에서 컴포넌트를 검색하고 설치까지 Claude Code 안에서 바로 처리해요.",

// Plugins (Phase 14)
"claude-mem":
  "세션 간 컨텍스트 유지가 중요한 장기 프로젝트예요. 매 세션 작업 내용을 자동 저장해서 다음 세션에 바로 이어서 작업할 수 있어요.",
superclaude:
  "체계적인 개발 워크플로가 필요한 프로젝트예요. 29개 /sc: 커맨드와 23개 전문 에이전트로 Claude Code를 구조화된 개발 플랫폼으로 확장해요.",
"frontend-design":
  "프론트엔드 UI 개발이 포함된 프로젝트예요. Anthropic 공식 플러그인으로 진부한 AI 디자인에서 벗어나 독창적인 타이포그래피와 레이아웃을 만들 수 있어요.",
```

Place these entries INSIDE the existing REASONS object, before the closing `};`. Maintain the comment grouping style (// MCP Servers, // Plugins) consistent with the file's existing section comments.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>REASONS object contains all 9 new IDs (fetch, time, markitdown, magic-mcp, n8n-mcp, shadcn-mcp, claude-mem, superclaude, frontend-design) with Korean reason strings following the established pattern. typecheck and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned reasonsEn export and run full CI (RSN-02)</name>
  <files>lib/i18n/plugins-en.ts</files>
  <action>
Delete the entire `export const reasonsEn: Record<string, string> = { ... };` block from lib/i18n/plugins-en.ts. This block starts at the line with `export const reasonsEn` (line ~209) and ends at the closing `};` (line ~261). Keep the `pluginDescEn` export completely intact — it IS used by plugins.test.ts (imported at line 3).

CRITICAL: Do NOT delete anything above the reasonsEn block. The file should end cleanly after the pluginDescEn closing `};` with a single trailing newline.

After deletion, run the full CI pipeline: `pnpm typecheck && pnpm lint && pnpm build && pnpm test` to confirm:
1. No consumer of reasonsEn exists (typecheck passes)
2. pluginDescEn import in plugins.test.ts still works (test passes)
3. Build succeeds with no dead code warnings
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build && pnpm test</automated>
  </verify>
  <done>reasonsEn export is completely removed from lib/i18n/plugins-en.ts. pluginDescEn export remains intact. pnpm typecheck, lint, build, and test all pass green.</done>
</task>

</tasks>

<verification>
1. `grep -c "fetch:\|time:\|markitdown:\|magic-mcp\|n8n-mcp\|shadcn-mcp\|claude-mem\|superclaude\|frontend-design" lib/plugin-reasons.ts` returns 9
2. `grep -c "reasonsEn" lib/i18n/plugins-en.ts` returns 0
3. `grep -c "pluginDescEn" lib/i18n/plugins-en.ts` returns 1 (export still present)
4. `pnpm typecheck && pnpm lint && pnpm build && pnpm test` all pass
</verification>

<success_criteria>
- All 9 new plugin IDs have Korean reason strings in REASONS object
- reasonsEn export is deleted from plugins-en.ts
- pluginDescEn export is untouched
- Full CI pipeline (typecheck + lint + build + test) passes
</success_criteria>

<output>
After completion, create `.planning/phases/16-reason-strings/16-01-SUMMARY.md`
</output>
