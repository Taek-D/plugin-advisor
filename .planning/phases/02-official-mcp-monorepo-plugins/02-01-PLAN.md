---
phase: 02-official-mcp-monorepo-plugins
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "sequential-thinking, brave-search, puppeteer 3개 플러그인의 서브디렉토리가 modelcontextprotocol/servers 모노레포에 존재함이 확인되었다"
    - "3개 플러그인의 install 명령어가 공식 claude mcp add 형식 및 @modelcontextprotocol/server-* 패키지명과 일치한다"
    - "3개 플러그인의 features, keywords가 각 서버의 실제 제공 기능을 정확히 반영한다"
    - "brave-search의 requiredSecrets가 정확히 기재되어 있다"
    - "puppeteer와 playwright의 conflicts 규칙이 검증되었다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for sequential-thinking, brave-search, puppeteer"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for sequential-thinking, brave-search, puppeteer"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "sequential-thinking|brave-search|puppeteer"
---

<objective>
Verify and update metadata for sequential-thinking, brave-search, and puppeteer plugins against the modelcontextprotocol/servers monorepo READMEs.

Purpose: Ensure these 3 MCP monorepo plugins have accurate descriptions, features, install commands, keywords, requiredSecrets, and conflicts based on their official source.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified metadata, verificationStatus set to "verified" for confirmed plugins.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-community-orchestration-plugins/01-01-SUMMARY.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns the executor needs, extracted from Phase 1 approach -->

From lib/plugins.ts — the update pattern:
- CORE_PLUGINS record: update `desc`, `longDesc`, `features`, `keywords`, `install`, `url`, `conflicts` fields
- PLUGIN_FIELD_OVERRIDES record: update `verificationStatus`, `officialStatus`, `difficulty`, `requiredSecrets`, `bestFor`, `avoidFor`, `installMode`, `prerequisites`
- Default verificationStatus is "partial" — update to "verified" after confirming against README

From lib/i18n/plugins-en.ts — the sync pattern:
- pluginDescEn: update `desc` and `longDesc` English translations to match updated Korean text
- reasonsEn: update recommendation reason text if features/description changed significantly

Plugin subdirectory URLs in monorepo:
- sequential-thinking: https://github.com/modelcontextprotocol/servers/tree/main/src/sequentialthinking
- brave-search: https://github.com/modelcontextprotocol/servers/tree/main/src/brave-search
- puppeteer: https://github.com/modelcontextprotocol/servers/tree/main/src/puppeteer
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for sequential-thinking, brave-search, puppeteer</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 3 plugins (sequential-thinking, brave-search, puppeteer), perform the following verification steps against the modelcontextprotocol/servers monorepo:

**Step 0: Fetch source data**
Use GitHub API or raw content fetch to retrieve the README.md and package.json from each plugin's subdirectory in modelcontextprotocol/servers:
- `src/sequentialthinking/` (note: directory name has no hyphen)
- `src/brave-search/`
- `src/puppeteer/`

If a subdirectory README is unavailable, try the monorepo root README for that server's section. Also check each server's package.json for the correct npm package name (@modelcontextprotocol/server-*).

**Step 1: VERIFY-01 — Confirm subdirectory existence**
Verify each plugin's subdirectory exists in the monorepo. Log the confirmation.

**Step 2: VERIFY-02 — Description accuracy**
Compare current `desc` and `longDesc` (Korean) against the README content. Update if the README reveals features, capabilities, or use cases not reflected in current descriptions. Keep descriptions concise and in the same style as Phase 1 updates.

**Step 3: VERIFY-03 — Features accuracy**
Compare current `features` array against the actual tools/capabilities listed in the README. Each feature entry should reflect a real, documented capability. Update as needed.

**Step 4: VERIFY-04 — Keywords accuracy**
Check if `keywords` reflect the actual use cases from the README. Add missing relevant keywords, remove misleading ones.

**Step 5: VERIFY-05 — Install command accuracy**
Verify the `install` array uses the correct format:
- Must use `claude mcp add {name} -- npx -y @modelcontextprotocol/server-{name}` pattern
- Verify the exact npm package name from package.json
- For puppeteer: confirm no additional args needed
- For brave-search: confirm if env var setup is part of install or separate

**Step 6: VERIFY-06 — Conflicts accuracy**
- brave-search: currently has `conflicts: ["tavily"]` — verify if this is a valid design decision (both are search engines, recommender should avoid suggesting both)
- puppeteer: currently has `conflicts: ["playwright"]` — verify this makes sense (both are browser automation, overlapping use case)
- sequential-thinking: currently has `conflicts: []` — confirm no conflicts

**Step 7: Update PLUGIN_FIELD_OVERRIDES**
- Set `verificationStatus: "verified"` for each confirmed plugin
- Set `officialStatus: "official"` for all 3 (they are from the official MCP monorepo)
- Verify `requiredSecrets` for brave-search (currently `["Brave Search API key"]`)
- Verify `difficulty`, `bestFor`, `avoidFor` are reasonable
- Verify `installMode` — brave-search needs "external-setup" (API key required), sequential-thinking and puppeteer may be simpler

**Step 8: Sync English translations**
Update `pluginDescEn` entries for all 3 plugins to match the updated Korean descriptions.
Update `reasonsEn` entries if the recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs — they are used as keys throughout the codebase
- Do NOT modify verified plugins from Phase 1 (omc, agency-agents, ralph) — out of scope
- Do NOT add or remove plugins — only verify/update existing metadata
- Do NOT use `any` type — per project conventions
- If a README is unavailable or repo appears dead, set verificationStatus to "unverified" instead of "verified" and document why in a code comment (same pattern as ralph in Phase 1)
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- sequential-thinking, brave-search, puppeteer metadata verified against monorepo READMEs
- verificationStatus updated to "verified" (or "unverified" with documented reason)
- officialStatus set to "official" for all 3
- English translations synced in plugins-en.ts
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- All 3 plugins have verificationStatus updated in PLUGIN_FIELD_OVERRIDES
- Install commands follow `claude mcp add {name} -- npx -y @modelcontextprotocol/server-{name}` pattern
- brave-search has requiredSecrets with correct API key name
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions
</verification>

<success_criteria>
- sequential-thinking, brave-search, puppeteer metadata matches their README in modelcontextprotocol/servers
- Install commands use correct @modelcontextprotocol/server-* package names
- requiredSecrets correctly lists API keys for brave-search
- verificationStatus updated to "verified" for all 3 (or "unverified" with documented reason)
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-official-mcp-monorepo-plugins/02-01-SUMMARY.md`
</output>
