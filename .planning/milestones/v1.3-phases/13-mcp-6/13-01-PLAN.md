---
phase: 13-mcp-6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "fetch가 /plugins MCP 탭에 표시된다"
    - "time이 /plugins MCP 탭에 표시된다"
    - "markitdown이 /plugins MCP 탭에 표시된다"
    - "/advisor에서 'http요청', 'url' 등 키워드 입력 시 fetch가 추천된다"
    - "/advisor에서 '시간', 'timezone' 등 키워드 입력 시 time이 추천된다"
    - "/advisor에서 '마크다운 변환', 'pdf' 등 키워드 입력 시 markitdown이 추천된다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "fetch, time, markitdown PluginSeed entries in CORE_PLUGINS + PLUGIN_FIELD_OVERRIDES"
      contains: "fetch:"
    - path: "lib/i18n/plugins-en.ts"
      provides: "English desc + longDesc for fetch, time, markitdown"
      contains: "fetch:"
  key_links:
    - from: "lib/plugins.ts (CORE_PLUGINS.fetch)"
      to: "/advisor scoring engine"
      via: "keywords array auto-matching"
      pattern: "keywords:.*fetch.*REST"
    - from: "lib/plugins.ts (CORE_PLUGINS.time)"
      to: "/advisor scoring engine"
      via: "keywords array auto-matching"
      pattern: "keywords:.*시간.*timezone"
    - from: "lib/plugins.ts (CORE_PLUGINS.markitdown)"
      to: "/advisor scoring engine"
      via: "keywords array auto-matching"
      pattern: "keywords:.*마크다운.*markdown"
---

<objective>
Register 3 official MCP servers (fetch, time, markitdown) in the plugin DB with verified metadata.

Purpose: Expand DB from 42 to 45 entries. These 3 are Python-based official/Microsoft servers with no API key requirements -- the simplest batch.
Output: 3 new PluginSeed entries + PLUGIN_FIELD_OVERRIDES + English translations
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mcp-6/13-CONTEXT.md
@.planning/phases/13-mcp-6/13-RESEARCH.md

<interfaces>
<!-- Key types and insertion patterns the executor needs. -->

From lib/plugins.ts (line 18-21):
```typescript
type PluginSeed = Omit<Plugin, keyof PluginOperationalFields>;
// Required fields: id, name, tag, color, category, githubRepo, desc, longDesc, url, install, features, conflicts, keywords
```

From lib/plugins.ts (line 23-35) -- DEFAULT_PLUGIN_FIELDS:
```typescript
// MCP servers get type: "mcp" automatically -- do NOT add type to CORE_PLUGINS
const DEFAULT_PLUGIN_FIELDS: PluginOperationalFields = {
  officialStatus: "community",
  verificationStatus: "partial",
  difficulty: "intermediate",
  // ...
  type: "mcp",
};
```

From lib/plugins.ts (line 37-38) -- PLUGIN_FIELD_OVERRIDES pattern:
```typescript
const PLUGIN_FIELD_OVERRIDES: Partial<Record<string, Partial<PluginOperationalFields>>> = {
  // Each entry overrides DEFAULT_PLUGIN_FIELDS for that plugin id
};
```

Category section headers pattern:
```typescript
// ─────────────────────────────────────────────
// Category Name
// ─────────────────────────────────────────────
```

Insertion points:
- `data` section: fetch goes after postgres entry (line ~1091), before Security section (line 1093)
- `integration` section: time goes after figma entry (line ~1322), before UI/UX section (line 1324)
- `documentation` section: markitdown goes after notion entry (line ~925), before Data section (line 927)

PLUGIN_FIELD_OVERRIDES: Add after existing entries (before closing `};`)

pluginDescEn pattern (lib/i18n/plugins-en.ts):
```typescript
export const pluginDescEn: Record<string, { desc: string; longDesc: string }> = {
  pluginId: { desc: "...", longDesc: "..." },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetch, time, markitdown to CORE_PLUGINS and PLUGIN_FIELD_OVERRIDES</name>
  <files>lib/plugins.ts</files>
  <action>
Add 3 PluginSeed entries to CORE_PLUGINS in their correct category sections, and add corresponding PLUGIN_FIELD_OVERRIDES entries. Do NOT add a `type` field to CORE_PLUGINS entries -- DEFAULT_PLUGIN_FIELDS injects `type: "mcp"` automatically.

**1. fetch (category: data) -- insert after postgres entry, before `// Security` section header**

CORE_PLUGINS entry:
```typescript
fetch: {
  id: "fetch",
  name: "Fetch MCP",
  tag: "FCH",
  color: "#0EA5E9",
  category: "data",
  githubRepo: "modelcontextprotocol/servers",
  desc: "URL 내용을 Markdown으로 변환 fetch. HTTP 요청, 웹 스크래핑, API 응답 조회.",
  longDesc:
    "Fetch MCP는 지정한 URL의 웹 페이지나 API 응답을 Markdown으로 변환해서 Claude Code에 전달해주는 공식 MCP 서버예요. 단순 HTTP GET뿐 아니라 커스텀 헤더, 페이지네이션(start_index), raw 모드(Markdown 변환 없이 원본 반환)도 지원해요. 로컬/내부 IP도 접근 가능하고, 로봇 이름 설정도 커스터마이즈할 수 있어요. Firecrawl과 달리 단일 URL 조회에 특화돼 있고 API 키가 필요 없어요.",
  url: "https://github.com/modelcontextprotocol/servers/tree/main/src/fetch",
  install: [
    "claude mcp add fetch -- uvx mcp-server-fetch",
  ],
  features: [
    "URL → Markdown 변환",
    "페이지네이션 지원 (start_index)",
    "raw 모드 (Markdown 변환 없이 원본)",
    "로컬/내부 IP 접근 가능",
  ],
  conflicts: [],
  keywords: [
    "http요청", "api호출", "url", "fetch", "REST",
    "웹페이지", "스크래핑", "scrape", "html",
    "get", "request", "웹", "web",
  ],
},
```

PLUGIN_FIELD_OVERRIDES entry:
```typescript
fetch: {
  officialStatus: "official",
  verificationStatus: "verified",
  difficulty: "beginner",
  installMode: "safe-copy",
  prerequisites: ["Python 또는 uvx (uv) 설치"],
  bestFor: ["단순 HTTP 데이터 조회", "웹 페이지 요약", "API 응답 파싱"],
},
```

**2. time (category: integration) -- insert after figma entry, before `// UI/UX` section header**

CORE_PLUGINS entry:
```typescript
time: {
  id: "time",
  name: "Time MCP",
  tag: "TIME",
  color: "#8B5CF6",
  category: "integration",
  githubRepo: "modelcontextprotocol/servers",
  desc: "현재 시간 조회 및 시간대 변환. 글로벌 팀 협업이나 타임존 처리에 유용.",
  longDesc:
    "Time MCP는 현재 시간 조회와 시간대 변환을 제공하는 공식 MCP 서버예요. get_current_time으로 특정 타임존의 현재 시간을, convert_time으로 시간대 간 변환을 할 수 있어요. LOCAL_TIMEZONE 환경변수나 --local-timezone 인자로 기본 시간대를 설정할 수 있어요. 글로벌 팀 협업, 스케줄링, 로그 타임스탬프 분석 같은 시간대 처리가 필요한 상황에서 유용해요.",
  url: "https://github.com/modelcontextprotocol/servers/tree/main/src/time",
  install: [
    "claude mcp add time -- uvx mcp-server-time",
  ],
  features: [
    "현재 시간 조회 (타임존 지정)",
    "시간대 변환 (소스→타깃)",
    "LOCAL_TIMEZONE 환경변수 지원",
    "IANA 타임존 식별자 사용",
  ],
  conflicts: [],
  keywords: [
    "시간", "time", "타임존", "timezone", "시간대", "시계",
    "날짜", "date", "utc", "변환", "convert",
    "글로벌", "global", "스케줄", "schedule",
  ],
},
```

PLUGIN_FIELD_OVERRIDES entry:
```typescript
time: {
  officialStatus: "official",
  verificationStatus: "verified",
  difficulty: "beginner",
  installMode: "safe-copy",
  prerequisites: ["Python 또는 uvx (uv) 설치"],
  bestFor: ["글로벌 팀 시간 조율", "타임존 변환", "로그 타임스탬프 분석"],
},
```

**3. markitdown (category: documentation) -- insert after notion entry, before `// Data` section header**

CORE_PLUGINS entry:
```typescript
markitdown: {
  id: "markitdown",
  name: "MarkItDown MCP",
  tag: "MID",
  color: "#2563EB",
  category: "documentation",
  githubRepo: "microsoft/markitdown",
  desc: "다양한 파일(PDF, DOCX, XLSX, 이미지 등)을 Markdown으로 변환. Microsoft 공식.",
  longDesc:
    "MarkItDown MCP는 Microsoft가 개발한 파일→Markdown 변환 서버예요. PDF, Word, Excel, PowerPoint, HTML, 이미지, 오디오 등 다양한 포맷을 Markdown으로 변환해줘요. convert_to_markdown 툴 하나로 http/https/file/data URI를 모두 지원해요. Claude Code에서 문서 파일을 직접 읽고 분석할 때 유용하지만, claude mcp add 단일 명령이 아닌 pip 설치 후 수동 설정이 필요해요.",
  url: "https://github.com/microsoft/markitdown/tree/main/packages/markitdown-mcp",
  install: [
    "pip install markitdown-mcp",
    "markitdown-mcp",
  ],
  features: [
    "PDF/DOCX/XLSX/PPTX → Markdown 변환",
    "이미지 OCR 및 오디오 전사",
    "http/https/file/data URI 지원",
    "Claude Code 문서 분석 연동",
  ],
  conflicts: [],
  keywords: [
    "마크다운", "markdown", "변환", "convert", "pdf", "docx", "워드", "word",
    "엑셀", "excel", "파워포인트", "ppt", "파일", "file",
    "문서", "document", "ocr",
  ],
},
```

PLUGIN_FIELD_OVERRIDES entry:
```typescript
markitdown: {
  officialStatus: "official",
  verificationStatus: "verified",
  difficulty: "intermediate",
  installMode: "manual-required",
  prerequisites: ["Python 설치", "pip 사용 가능 환경"],
  bestFor: ["문서 파일 분석", "PDF/Office 파일 Markdown 변환", "다포맷 문서 처리"],
},
```

**Critical rules:**
- Do NOT add `type` field to any CORE_PLUGINS entry (PluginSeed has no type)
- Do NOT use npx for fetch/time/markitdown (they are Python-based)
- install commands are verbatim from verified READMEs -- do NOT modify
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck</automated>
  </verify>
  <done>fetch, time, markitdown entries exist in CORE_PLUGINS with correct categories, install commands match verified READMEs, PLUGIN_FIELD_OVERRIDES set with verificationStatus: "verified", pnpm typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Add English translations for fetch, time, markitdown</name>
  <files>lib/i18n/plugins-en.ts</files>
  <action>
Add 3 entries to the `pluginDescEn` object in lib/i18n/plugins-en.ts. Each entry needs `desc` (1 sentence) and `longDesc` (2-3 sentences matching the Korean longDesc content).

```typescript
fetch: {
  desc: "Fetches URLs and converts content to Markdown. Essential for HTTP requests, web scraping, and API response inspection.",
  longDesc: "Fetch MCP is an official MCP server that retrieves web pages and API responses, converting them to Markdown for Claude Code. It supports pagination (start_index), raw mode (no Markdown conversion), custom headers, and local/internal IP access. Unlike Firecrawl which focuses on full-site crawling, Fetch specializes in single URL retrieval with no API key required.",
},
time: {
  desc: "Current time lookup and timezone conversion. Useful for global team coordination and timestamp analysis.",
  longDesc: "Time MCP is an official MCP server providing current time queries and timezone conversion. get_current_time returns the current time for any IANA timezone, and convert_time converts between timezones. Supports LOCAL_TIMEZONE environment variable for default timezone configuration. Useful for global team scheduling and log timestamp analysis.",
},
markitdown: {
  desc: "Converts various files (PDF, DOCX, XLSX, images, audio) to Markdown. Official Microsoft project.",
  longDesc: "MarkItDown MCP is a Microsoft-developed file-to-Markdown conversion server. It converts PDF, Word, Excel, PowerPoint, HTML, images, audio, and more into Markdown via a single convert_to_markdown tool supporting http/https/file/data URIs. Requires pip install and manual MCP configuration (no one-line claude mcp add command available).",
},
```

Also add corresponding entries to the `reasonsEn` object at the end of the file (same pattern as existing entries like `notion`, `firecrawl`):

```typescript
fetch: "HTTP data retrieval or web content inspection needed. Fetch MCP converts any URL to Markdown — ideal for API responses, web pages, and simple scraping without API keys.",
time: "Timezone handling or time conversion needed. Time MCP provides current time queries and timezone conversion using IANA identifiers — useful for global teams and log analysis.",
markitdown: "Document file analysis needed. MarkItDown converts PDF, DOCX, XLSX, PPTX, images, and audio to Markdown — official Microsoft tool for multi-format document processing.",
```
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm test -- plugins</automated>
  </verify>
  <done>pluginDescEn has fetch, time, markitdown entries with desc + longDesc. reasonsEn has corresponding entries. pnpm typecheck and pnpm test pass.</done>
</task>

</tasks>

<verification>
```bash
cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm test -- plugins
```

Manual spot-checks:
- `PLUGINS.fetch.type === "mcp"` (auto-injected)
- `PLUGINS.fetch.category === "data"`
- `PLUGINS.time.category === "integration"`
- `PLUGINS.markitdown.category === "documentation"`
- `PLUGINS.fetch.install[0]` contains `uvx` (not npx)
- `PLUGINS.markitdown.install[0]` contains `pip` (not npx)
- `Object.keys(PLUGINS).length >= 45`
</verification>

<success_criteria>
- 3 new MCP servers (fetch, time, markitdown) visible in PLUGINS export
- Each has type "mcp" (auto-injected), correct category per user decision
- Install commands match verified READMEs verbatim
- English translations complete in pluginDescEn and reasonsEn
- pnpm typecheck passes with no errors
- pnpm test passes (existing >= 42 threshold still satisfied at 45)
</success_criteria>

<output>
After completion, create `.planning/phases/13-mcp-6/13-01-SUMMARY.md`
</output>
