---
phase: 13-mcp-6
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements:
  - MCP-04
  - MCP-05
  - MCP-06
must_haves:
  truths:
    - "magic-mcp가 /plugins MCP 탭에 표시된다"
    - "n8n-mcp가 /plugins MCP 탭에 표시된다"
    - "shadcn-mcp가 /plugins MCP 탭에 표시된다"
    - "/advisor에서 'UI 생성', 'ai component' 등 키워드 입력 시 magic-mcp가 추천된다"
    - "/advisor에서 '자동화', 'workflow' 등 키워드 입력 시 n8n-mcp가 추천된다"
    - "/advisor에서 'shadcn', '컴포넌트 라이브러리' 등 키워드 입력 시 shadcn-mcp가 추천된다"
    - "magic-mcp의 requiredSecrets에 TWENTY_FIRST_API_KEY가 포함된다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "magic-mcp, n8n-mcp, shadcn-mcp PluginSeed entries in CORE_PLUGINS + PLUGIN_FIELD_OVERRIDES"
      contains: "magic-mcp:"
    - path: "lib/i18n/plugins-en.ts"
      provides: "English desc + longDesc for magic-mcp, n8n-mcp, shadcn-mcp"
      contains: "magic-mcp:"
  key_links:
    - from: "lib/plugins.ts (CORE_PLUGINS['magic-mcp'])"
      to: "/advisor scoring engine"
      via: "keywords array auto-matching"
      pattern: "keywords:.*ai.*컴포넌트"
    - from: "lib/plugins.ts (CORE_PLUGINS['n8n-mcp'])"
      to: "/advisor scoring engine"
      via: "keywords array auto-matching"
      pattern: "keywords:.*자동화.*workflow"
    - from: "lib/plugins.ts (CORE_PLUGINS['shadcn-mcp'])"
      to: "/advisor scoring engine"
      via: "keywords array auto-matching"
      pattern: "keywords:.*shadcn.*컴포넌트"
---

<objective>
Register 3 MCP servers (magic-mcp, n8n-mcp, shadcn-mcp) in the plugin DB with verified metadata.

Purpose: Complete Phase 13 DB expansion from 45 to 48 entries. These 3 have more complex install patterns (API key, env flags, pnpm dlx).
Output: 3 new PluginSeed entries + PLUGIN_FIELD_OVERRIDES + English translations
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mcp-6/13-CONTEXT.md
@.planning/phases/13-mcp-6/13-RESEARCH.md
@.planning/phases/13-mcp-6/13-01-SUMMARY.md

<interfaces>
<!-- Key types and insertion patterns the executor needs. -->

From lib/plugins.ts:
```typescript
type PluginSeed = Omit<Plugin, keyof PluginOperationalFields>;
// Required fields: id, name, tag, color, category, githubRepo, desc, longDesc, url, install, features, conflicts, keywords
// Do NOT add type field -- DEFAULT_PLUGIN_FIELDS injects type: "mcp"
```

PLUGIN_FIELD_OVERRIDES pattern:
```typescript
const PLUGIN_FIELD_OVERRIDES: Partial<Record<string, Partial<PluginOperationalFields>>> = {
  // installMode: "safe-copy" | "manual-required" | "external-setup"
  // requiredSecrets: string[]  -- only for servers needing API keys
};
```

Insertion points (after Plan 01 adds fetch, time, markitdown):
- `ui-ux` section (existing, has uiux entry): magic-mcp and shadcn-mcp go after uiux entry, before `// DevOps` section header
- `workflow` section: n8n-mcp goes after linear entry, before `// Code Quality` section header

pluginDescEn + reasonsEn pattern (lib/i18n/plugins-en.ts):
```typescript
pluginId: { desc: "...", longDesc: "..." },
// reasonsEn at end of file
pluginId: "...",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add magic-mcp, n8n-mcp, shadcn-mcp to CORE_PLUGINS and PLUGIN_FIELD_OVERRIDES</name>
  <files>lib/plugins.ts</files>
  <action>
Add 3 PluginSeed entries to CORE_PLUGINS in their correct category sections, and add corresponding PLUGIN_FIELD_OVERRIDES entries. Do NOT add a `type` field to CORE_PLUGINS entries.

**1. magic-mcp (category: ui-ux) -- insert after uiux entry in the existing UI/UX section, before `// DevOps` section header**

CORE_PLUGINS entry:
```typescript
"magic-mcp": {
  id: "magic-mcp",
  name: "Magic MCP",
  tag: "MGC",
  color: "#7C3AED",
  category: "ui-ux",
  githubRepo: "21st-dev/magic-mcp",
  desc: "자연어로 AI UI 컴포넌트 생성. 21st.dev 라이브러리 기반 즉시 사용 가능한 컴포넌트.",
  longDesc:
    "Magic MCP는 21st.dev가 제공하는 AI UI 컴포넌트 생성 서버예요. /ui 커맨드로 자연어를 입력하면 21st.dev 컴포넌트 라이브러리에서 적합한 UI 컴포넌트를 생성하거나 추천해줘요. React/Next.js 프로젝트에서 버튼, 카드, 모달 같은 UI를 빠르게 만들 때 유용해요. TWENTY_FIRST_API_KEY가 필요하며 21st.dev Magic Console에서 발급받을 수 있어요. shadcn-mcp와 보완 관계 — shadcn은 기존 컴포넌트 라이브러리 접근, magic은 AI 기반 생성이에요.",
  url: "https://github.com/21st-dev/magic-mcp",
  install: [
    "npx @21st-dev/cli@latest install claude --api-key <key>",
  ],
  features: [
    "자연어 UI 컴포넌트 생성 (/ui 커맨드)",
    "21st.dev 컴포넌트 라이브러리 접근",
    "React/Next.js 프로젝트 지원",
    "즉시 사용 가능한 코드 출력",
  ],
  conflicts: [],
  keywords: [
    "ai ui", "ui 생성", "컴포넌트 생성", "ai component", "magic",
    "21st", "자동 ui", "프론트엔드 생성", "react 컴포넌트",
    "generate", "생성", "frontend", "ui",
  ],
},
```

PLUGIN_FIELD_OVERRIDES entry:
```typescript
"magic-mcp": {
  verificationStatus: "verified",
  difficulty: "beginner",
  installMode: "external-setup",
  requiredSecrets: ["TWENTY_FIRST_API_KEY (21st.dev Magic Console에서 발급)"],
  bestFor: ["AI 기반 UI 컴포넌트 빠른 생성", "React/Next.js UI 프로토타이핑"],
  avoidFor: ["API 키 없는 환경"],
},
```

**2. shadcn-mcp (category: ui-ux) -- insert after magic-mcp in the UI/UX section, before `// DevOps` section header**

CORE_PLUGINS entry:
```typescript
"shadcn-mcp": {
  id: "shadcn-mcp",
  name: "shadcn/ui MCP",
  tag: "SHAD",
  color: "#18181B",
  category: "ui-ux",
  githubRepo: "shadcn-ui/ui",
  desc: "shadcn/ui 공식 MCP. 컴포넌트 검색, 설치, 레지스트리 조회를 Claude Code에서 직접.",
  longDesc:
    "shadcn/ui MCP는 shadcn/ui 팀(Vercel)이 공식 제공하는 MCP 서버예요. Claude Code에서 shadcn/ui 컴포넌트를 검색하고 설치하고 레지스트리를 조회할 수 있어요. pnpm dlx shadcn@latest mcp init --client claude 명령으로 한 줄 설치가 가능해요. API 키 없이 공개 레지스트리 기준으로 동작해요. magic-mcp와 보완 관계 — magic은 AI 생성, shadcn은 기존 컴포넌트 라이브러리 접근이에요.",
  url: "https://ui.shadcn.com/docs/mcp",
  install: [
    "pnpm dlx shadcn@latest mcp init --client claude",
  ],
  features: [
    "shadcn/ui 컴포넌트 검색",
    "컴포넌트 설치 자동화",
    "레지스트리 조회",
    "공식 shadcn/ui 팀 제공",
  ],
  conflicts: [],
  keywords: [
    "shadcn", "ui 라이브러리", "컴포넌트 라이브러리", "shadcn-ui",
    "radix", "tailwind ui", "component library", "디자인 시스템",
    "design system", "ui kit", "리액트 컴포넌트", "react component",
  ],
},
```

PLUGIN_FIELD_OVERRIDES entry:
```typescript
"shadcn-mcp": {
  officialStatus: "official",
  verificationStatus: "verified",
  difficulty: "beginner",
  installMode: "safe-copy",
  bestFor: ["shadcn/ui 컴포넌트 검색 및 설치", "React+Tailwind 프로젝트 UI 구성"],
},
```

**3. n8n-mcp (category: workflow) -- insert after linear entry, before `// Code Quality` section header**

CORE_PLUGINS entry:
```typescript
"n8n-mcp": {
  id: "n8n-mcp",
  name: "n8n MCP",
  tag: "N8N",
  color: "#FF6D5A",
  category: "workflow",
  githubRepo: "czlonkowski/n8n-mcp",
  desc: "n8n 워크플로 자동화 연동. n8n 없이도 문서/검증 모드(7개 core tools)로 사용 가능.",
  longDesc:
    "n8n MCP는 n8n 워크플로 자동화 플랫폼을 Claude Code에서 연동하는 서버예요. n8n 인스턴스 없이도 7개 core tools(문서 검증, 워크플로 설계 지원)로 기본 사용이 가능해요. n8n API Key를 연동하면 13개 추가 tools로 워크플로 생성, 실행, 모니터링까지 할 수 있어요. MCP_MODE=stdio와 환경변수 설정으로 Claude Code에 최적화된 설정을 제공해요.",
  url: "https://github.com/czlonkowski/n8n-mcp",
  install: [
    "claude mcp add n8n-mcp -e MCP_MODE=stdio -e LOG_LEVEL=error -e DISABLE_CONSOLE_OUTPUT=true -- npx n8n-mcp",
  ],
  features: [
    "워크플로 문서 검증 (core 7개 tools)",
    "n8n 워크플로 생성/실행/모니터링 (API 연동 시)",
    "MCP_MODE=stdio 최적화",
    "n8n 없이 기본 모드 사용 가능",
  ],
  conflicts: [],
  keywords: [
    "n8n", "자동화", "automation", "워크플로", "workflow",
    "노코드", "nocode", "통합", "integration", "파이프라인",
    "pipeline", "트리거", "trigger", "연동", "zapier",
  ],
},
```

PLUGIN_FIELD_OVERRIDES entry:
```typescript
"n8n-mcp": {
  verificationStatus: "verified",
  difficulty: "intermediate",
  installMode: "safe-copy",
  bestFor: ["워크플로 자동화 설계", "n8n 기반 통합 자동화", "비개발자 업무 자동화"],
},
```

**Critical rules:**
- Do NOT add `type` field to any CORE_PLUGINS entry
- magic-mcp: `requiredSecrets` MUST include TWENTY_FIRST_API_KEY, `installMode` MUST be "external-setup"
- n8n-mcp: `requiredSecrets: []` (API 없이 기본 동작), `installMode: "safe-copy"` (기본 문서 모드)
- shadcn-mcp: install uses `pnpm dlx` NOT npx/npm (공식 shadcn/ui CLI 패턴)
- install commands are verbatim from verified READMEs -- do NOT modify
- magic-mcp and shadcn-mcp: `conflicts: []` (보완 관계, 충돌 아님 -- user decision)
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck</automated>
  </verify>
  <done>magic-mcp, n8n-mcp, shadcn-mcp entries exist in CORE_PLUGINS with correct categories, install commands match verified READMEs, PLUGIN_FIELD_OVERRIDES set with appropriate requiredSecrets and installMode, pnpm typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Add English translations for magic-mcp, n8n-mcp, shadcn-mcp</name>
  <files>lib/i18n/plugins-en.ts</files>
  <action>
Add 3 entries to the `pluginDescEn` object and 3 entries to the `reasonsEn` object in lib/i18n/plugins-en.ts.

pluginDescEn entries:
```typescript
"magic-mcp": {
  desc: "AI-powered UI component generation via natural language. Leverages the 21st.dev component library for instant, production-ready components.",
  longDesc: "Magic MCP is an AI UI component generation server by 21st.dev. Use the /ui command with natural language to generate or discover UI components from the 21st.dev library. Great for quickly building buttons, cards, modals, and more in React/Next.js projects. Requires TWENTY_FIRST_API_KEY from the 21st.dev Magic Console. Complements shadcn-mcp — magic generates via AI, shadcn accesses existing component libraries.",
},
"shadcn-mcp": {
  desc: "Official shadcn/ui MCP server. Search, install, and browse shadcn/ui components directly from Claude Code.",
  longDesc: "shadcn/ui MCP is the official MCP server provided by the shadcn/ui team (Vercel). It enables searching, installing, and browsing the shadcn/ui component registry directly from Claude Code. One-line install via pnpm dlx shadcn@latest mcp init --client claude. No API key needed — uses the public registry. Complements magic-mcp — shadcn provides library access, magic provides AI generation.",
},
"n8n-mcp": {
  desc: "n8n workflow automation integration. Works without n8n instance in documentation/verification mode with 7 core tools.",
  longDesc: "n8n MCP integrates the n8n workflow automation platform with Claude Code. It works without an n8n instance using 7 core tools for documentation and workflow design verification. Connecting an n8n API Key unlocks 13 additional tools for workflow creation, execution, and monitoring. Configured with MCP_MODE=stdio for optimal Claude Code integration.",
},
```

reasonsEn entries:
```typescript
"magic-mcp": "AI-powered UI component generation needed. Magic MCP creates production-ready React/Next.js components from natural language via 21st.dev's library — requires TWENTY_FIRST_API_KEY.",
"shadcn-mcp": "shadcn/ui component library access needed. Official shadcn/ui MCP enables component search, installation, and registry browsing directly from Claude Code — no API key required.",
"n8n-mcp": "Workflow automation design or n8n integration needed. n8n MCP provides 7 core tools without an n8n instance, plus 13 additional tools when connected to n8n's API.",
```
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build && pnpm test</automated>
  </verify>
  <done>pluginDescEn has magic-mcp, n8n-mcp, shadcn-mcp entries with desc + longDesc. reasonsEn has corresponding entries. Full CI suite (typecheck + lint + build + test) passes. Total PLUGINS count is 48.</done>
</task>

</tasks>

<verification>
```bash
cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build && pnpm test
```

Manual spot-checks:
- `PLUGINS["magic-mcp"].type === "mcp"` (auto-injected)
- `PLUGINS["magic-mcp"].category === "ui-ux"`
- `PLUGINS["magic-mcp"].requiredSecrets` includes TWENTY_FIRST_API_KEY
- `PLUGINS["n8n-mcp"].category === "workflow"`
- `PLUGINS["n8n-mcp"].requiredSecrets` is `[]`
- `PLUGINS["shadcn-mcp"].category === "ui-ux"`
- `PLUGINS["shadcn-mcp"].install[0]` contains `pnpm dlx` (not npx)
- `Object.keys(PLUGINS).length >= 48`
</verification>

<success_criteria>
- 3 new MCP servers (magic-mcp, n8n-mcp, shadcn-mcp) visible in PLUGINS export
- Each has type "mcp" (auto-injected), correct category per user decision
- magic-mcp has requiredSecrets with TWENTY_FIRST_API_KEY, installMode "external-setup"
- n8n-mcp has empty requiredSecrets, installMode "safe-copy"
- shadcn-mcp uses pnpm dlx install, officialStatus "official"
- Install commands match verified READMEs verbatim
- English translations complete in pluginDescEn and reasonsEn
- Full CI suite passes: pnpm typecheck, lint, build, test
- Total PLUGINS count is 48 (42 existing + 6 new)
</success_criteria>

<output>
After completion, create `.planning/phases/13-mcp-6/13-02-SUMMARY.md`
</output>
