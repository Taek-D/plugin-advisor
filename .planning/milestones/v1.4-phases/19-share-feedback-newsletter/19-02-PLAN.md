---
phase: 19-share-feedback-newsletter
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - components/ShareResultButton.tsx
  - components/PluginAdvisorApp.tsx
  - components/OptimizerApp.tsx
autonomous: true
requirements:
  - SHAR-01
  - SHAR-02
  - SHAR-03

must_haves:
  truths:
    - "Optimizer 결과 화면에서 공유 버튼이 표시되고 클릭하면 URL이 공유/복사된다"
    - "Advisor 결과 화면에서 공유 버튼이 표시되고 클릭하면 URL이 공유/복사된다"
    - "데스크톱에서 Copy->Check 아이콘 전환이 2초간 표시된다"
    - "모바일에서 Web Share API가 호출된다"
  artifacts:
    - path: "components/ShareResultButton.tsx"
      provides: "공유 버튼 클라이언트 컴포넌트"
      exports: ["default"]
      min_lines: 20
    - path: "components/OptimizerApp.tsx"
      provides: "ShareResultButton 통합"
      contains: "ShareResultButton"
    - path: "components/PluginAdvisorApp.tsx"
      provides: "ShareResultButton 통합"
      contains: "ShareResultButton"
  key_links:
    - from: "components/ShareResultButton.tsx"
      to: "lib/share-utils.ts"
      via: "shareResult() import"
      pattern: "import.*shareResult.*share-utils"
    - from: "components/ShareResultButton.tsx"
      to: "lib/analytics.ts"
      via: "trackEvent('result_share')"
      pattern: "trackEvent.*result_share"
    - from: "components/OptimizerApp.tsx"
      to: "components/ShareResultButton.tsx"
      via: "JSX import and render"
      pattern: "<ShareResultButton"
    - from: "components/PluginAdvisorApp.tsx"
      to: "components/ShareResultButton.tsx"
      via: "JSX import and render"
      pattern: "<ShareResultButton"
---

<objective>
ShareResultButton 컴포넌트를 만들고, Optimizer 결과 화면과 Advisor 결과 화면에 통합한다. 데스크톱에서는 URL 클립보드 복사 + Copy->Check 전환, 모바일에서는 Web Share API 네이티브 공유 시트를 사용한다.

Purpose: 사용자가 추천/분석 결과를 SNS로 공유하여 사이트 트래픽을 유발하고, OG 이미지가 소셜 프리뷰에 자동 표시되게 한다.
Output: ShareResultButton.tsx + OptimizerApp/PluginAdvisorApp 통합
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-share-feedback-newsletter/19-RESEARCH.md
@.planning/phases/19-share-feedback-newsletter/19-01-SUMMARY.md

@components/OptimizerApp.tsx
@components/PluginAdvisorApp.tsx
@components/ResultsPanel.tsx
@lib/share-utils.ts
@lib/i18n/types.ts

<interfaces>
<!-- From Plan 19-01 outputs -->

From lib/share-utils.ts (created in 19-01):
```typescript
export type ShareOutcome = "native" | "clipboard" | "error";
export async function shareResult(url: string, title?: string): Promise<ShareOutcome>;
```

From lib/analytics.ts (extended in 19-01):
```typescript
export function trackEvent(event: EventName, payload?: EventPayload): void;
// EventName includes "result_share"
```

From lib/i18n/types.ts (extended in 19-01):
```typescript
share: {
  button: string;   // "공유하기" / "Share"
  copied: string;   // "복사됨" / "Copied"
  title: string;    // navigator.share title
};
```

From components/OptimizerApp.tsx:
```typescript
// Line 197: Results section — ShareResultButton goes ABOVE ResultsPanel
{analysisState === "done" && result && (
  <ResultsPanel result={result} />
)}
```

From components/PluginAdvisorApp.tsx:
```typescript
// Line 375: Result section — ShareResultButton goes inside the result container
{step === "result" && result && (
  <div className="animate-fade-in">
    <div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_22rem]">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShareResultButton 컴포넌트 생성</name>
  <files>components/ShareResultButton.tsx</files>
  <action>
`components/ShareResultButton.tsx`를 'use client' 컴포넌트로 생성한다.

**구현 사양:**
- `shareResult()`를 `lib/share-utils`에서 import
- `trackEvent()`를 `lib/analytics`에서 import
- `useI18n()`으로 `t.share` 번역 키 사용
- lucide-react에서 `Share2`, `Copy`, `Check` 아이콘 import
- shadcn `Button` (variant="outline", size="sm") 사용

**동작:**
1. 클릭 시 `shareResult(window.location.href, t.share.title)` 호출
2. `trackEvent("result_share", { outcome })` 추적
3. outcome이 "clipboard"이면 `copied` state를 true로 → 2초 후 false로 리셋
4. copied 상태에서: `<Check className="h-3.5 w-3.5 mr-1.5" />` + `t.share.copied`
5. 기본 상태에서: `<Share2 className="h-3.5 w-3.5 mr-1.5" />` + `t.share.button`

**코드 패턴 (연구에서 검증됨):**
```typescript
"use client";

import { useState } from "react";
import { Share2, Check } from "lucide-react";
import { Button } from "@/components/ui/button";
import { shareResult } from "@/lib/share-utils";
import { trackEvent } from "@/lib/analytics";
import { useI18n } from "@/lib/i18n";

export default function ShareResultButton() {
  const { t } = useI18n();
  const [copied, setCopied] = useState(false);

  const handleShare = async () => {
    const outcome = await shareResult(window.location.href, t.share.title);
    trackEvent("result_share", { outcome });
    if (outcome === "clipboard") {
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    }
  };

  return (
    <Button variant="outline" size="sm" onClick={handleShare}>
      {copied
        ? <><Check className="h-3.5 w-3.5 mr-1.5" />{t.share.copied}</>
        : <><Share2 className="h-3.5 w-3.5 mr-1.5" />{t.share.button}</>
      }
    </Button>
  );
}
```

**주의:** props로 URL을 받지 않음 — `window.location.href`를 클릭 핸들러 안에서 직접 사용 (OG 이미지가 해당 페이지 URL에 이미 바인딩됨). 이렇게 하면 부모 컴포넌트에서 URL을 전달할 필요가 없어 통합이 간단해짐.
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm typecheck</automated>
  </verify>
  <done>ShareResultButton.tsx가 share-utils의 shareResult()를 사용하고, Copy->Check 인라인 상태 전환과 analytics 추적이 구현됨.</done>
</task>

<task type="auto">
  <name>Task 2: OptimizerApp + PluginAdvisorApp에 ShareResultButton 통합</name>
  <files>components/OptimizerApp.tsx, components/PluginAdvisorApp.tsx</files>
  <action>
**OptimizerApp.tsx (SHAR-01):**
1. 상단에 `import ShareResultButton from "./ShareResultButton";` 추가
2. 결과 섹션 (line ~197 `{analysisState === "done" && result && (`) 내부에서, `<ResultsPanel result={result} />` 바로 위에 공유 버튼 행 추가:
```tsx
{analysisState === "done" && result && (
  <>
    <div className="flex justify-end">
      <ShareResultButton />
    </div>
    <ResultsPanel result={result} />
  </>
)}
```
기존 `<ResultsPanel result={result} />`를 Fragment로 감싸고, 그 위에 우측 정렬된 ShareResultButton을 배치.

**PluginAdvisorApp.tsx (SHAR-02):**
1. 상단에 `import ShareResultButton from "./ShareResultButton";` 추가
2. 결과 섹션 (line ~375 `{step === "result" && result && (`) 내부에서, `<div className="animate-fade-in">` 바로 다음, grid div 이전에 공유 버튼 행 추가:
```tsx
{step === "result" && result && (
  <div className="animate-fade-in">
    <div className="mb-4 flex justify-end">
      <ShareResultButton />
    </div>
    <div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_22rem]">
```

**두 파일 모두:**
- 기존 JSX 구조를 최소한으로만 변경 (import + JSX 삽입)
- 기존 로직/상태 관리는 건드리지 않음
  </action>
  <verify>
    <automated>cd /e/프로젝트/plugin-advisor && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>OptimizerApp 결과 화면과 PluginAdvisorApp 결과 화면 모두에 ShareResultButton이 렌더링됨. typecheck + lint 통과.</done>
</task>

</tasks>

<verification>
pnpm typecheck && pnpm lint && pnpm build
</verification>

<success_criteria>
- ShareResultButton.tsx가 'use client' 컴포넌트로 존재하고, shareResult() + trackEvent("result_share") + Copy->Check 상태 전환 구현
- OptimizerApp.tsx에서 analysisState === "done" 일 때 ShareResultButton 렌더링
- PluginAdvisorApp.tsx에서 step === "result" 일 때 ShareResultButton 렌더링
- pnpm typecheck + lint + build 모두 통과
</success_criteria>

<output>
After completion, create `.planning/phases/19-share-feedback-newsletter/19-02-SUMMARY.md`
</output>
