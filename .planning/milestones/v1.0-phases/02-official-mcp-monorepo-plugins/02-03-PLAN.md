---
phase: 02-official-mcp-monorepo-plugins
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "github, slack 2개 플러그인의 서브디렉토리가 modelcontextprotocol/servers 모노레포에 존재함이 확인되었다"
    - "2개 플러그인의 install 명령어가 공식 claude mcp add 형식 및 @modelcontextprotocol/server-* 패키지명과 일치한다"
    - "2개 플러그인의 features, keywords가 각 서버의 실제 제공 기능을 정확히 반영한다"
    - "github의 requiredSecrets(GitHub token)가 정확히 기재되어 있다"
    - "slack의 requiredSecrets(Slack workspace token)가 정확히 기재되어 있다"
    - "Phase 2의 9개 플러그인 모두 verificationStatus가 업데이트되었다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for github, slack — completing all 9 Phase 2 plugins"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for github, slack"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "github|slack"
---

<objective>
Verify and update metadata for github and slack plugins against the modelcontextprotocol/servers monorepo READMEs, completing Phase 2 verification of all 9 MCP monorepo plugins.

Purpose: Ensure the final 2 MCP monorepo plugins (both requiring API tokens) have accurate descriptions, features, install commands, keywords, and requiredSecrets based on their official source.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified metadata. All 9 Phase 2 plugins now verified.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-official-mcp-monorepo-plugins/02-02-SUMMARY.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns from prior plans -->

From lib/plugins.ts — same update pattern:
- CORE_PLUGINS: update desc, longDesc, features, keywords, install, url, conflicts
- PLUGIN_FIELD_OVERRIDES: update verificationStatus, officialStatus, difficulty, requiredSecrets, bestFor, avoidFor, installMode

Plugin subdirectory URLs in monorepo:
- github: https://github.com/modelcontextprotocol/servers/tree/main/src/github
- slack: https://github.com/modelcontextprotocol/servers/tree/main/src/slack

Current special fields to verify:
- github: officialStatus "official", installMode "external-setup", requiredSecrets ["GitHub token"]
- slack: officialStatus "official", installMode "external-setup", requiredSecrets ["Slack workspace token"]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for github, slack</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 2 plugins (github, slack), perform the following verification steps against the modelcontextprotocol/servers monorepo:

**Step 0: Fetch source data**
Use GitHub API or raw content fetch to retrieve the README.md and package.json from each plugin's subdirectory:
- `src/github/`
- `src/slack/`

**Step 1: VERIFY-01 — Confirm subdirectory existence**
Verify each plugin's subdirectory exists in the monorepo. Log the confirmation.

**Step 2: VERIFY-02 — Description accuracy**
Compare current `desc` and `longDesc` (Korean) against the README content. Update if the README reveals features, capabilities, or use cases not reflected in current descriptions.

Special attention:
- github: verify the actual GitHub API operations supported (PRs, issues, files, branches, repos, etc.)
- slack: verify the actual Slack API operations supported (channels, messages, threads, users, etc.)

**Step 3: VERIFY-03 — Features accuracy**
Compare current `features` array against the actual tools listed in the README.
- github: current features are ["PR 생성/리뷰", "이슈 관리", "코드 검색", "브랜치/릴리스 관리"] — verify these match the documented tools
- slack: current features are ["채널 메시지 관리", "스레드 읽기/쓰기", "사용자 검색", "배포 알림 자동화"] — verify these match the documented tools

**Step 4: VERIFY-04 — Keywords accuracy**
Check if `keywords` reflect actual use cases from the README. Add missing relevant keywords, remove misleading ones.

**Step 5: VERIFY-05 — Install command accuracy**
Verify install commands:
- github: `claude mcp add github -- npx -y @modelcontextprotocol/server-github` — verify package name and check if env var (GITHUB_PERSONAL_ACCESS_TOKEN or similar) needs to be documented
- slack: `claude mcp add slack -- npx -y @modelcontextprotocol/server-slack` — verify package name and check if env var (SLACK_BOT_TOKEN or similar) needs to be documented

**Step 6: VERIFY-06 — Conflicts accuracy**
Both currently have `conflicts: []`. Confirm no logical conflicts with other plugins.

**Step 7: Update PLUGIN_FIELD_OVERRIDES**
- Set `verificationStatus: "verified"` for both
- Confirm `officialStatus: "official"` for both
- Verify `requiredSecrets` — check the exact env var name from README:
  - github: currently ["GitHub token"] — update to match the exact env var name if README specifies (e.g., "GITHUB_PERSONAL_ACCESS_TOKEN")
  - slack: currently ["Slack workspace token"] — update to match the exact env var name if README specifies (e.g., "SLACK_BOT_TOKEN", "SLACK_TEAM_ID")
- Verify `installMode: "external-setup"` is correct for both (API tokens required)

**Step 8: Sync English translations**
Update `pluginDescEn` entries for both plugins to match updated Korean descriptions.
Update `reasonsEn` entries if the recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs
- Do NOT modify plugins verified in Plans 01 or 02
- Do NOT use `any` type
- If a README is unavailable, set verificationStatus to "unverified" with a code comment
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- github, slack metadata verified against monorepo READMEs
- verificationStatus updated to "verified" (or "unverified" with documented reason)
- officialStatus confirmed as "official" for both
- requiredSecrets verified with correct env var names from README
- English translations synced in plugins-en.ts
- All 9 Phase 2 plugins now have updated verificationStatus
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- All 9 Phase 2 plugins (sequential-thinking, brave-search, puppeteer, filesystem, git, postgres, memory, github, slack) have verificationStatus updated
- github and slack have correct requiredSecrets with exact env var names
- Install commands follow correct format
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions
</verification>

<success_criteria>
- github, slack metadata matches their README in modelcontextprotocol/servers
- requiredSecrets correctly lists exact API key/token names for both
- verificationStatus updated to "verified" for both (or "unverified" with documented reason)
- All 9 Phase 2 plugins are now verified — Phase 2 complete
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-official-mcp-monorepo-plugins/02-03-SUMMARY.md`
</output>
