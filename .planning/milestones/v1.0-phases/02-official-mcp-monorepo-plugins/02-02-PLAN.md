---
phase: 02-official-mcp-monorepo-plugins
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/plugins.ts
  - lib/i18n/plugins-en.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06]

must_haves:
  truths:
    - "filesystem, git, postgres, memory 4개 플러그인의 서브디렉토리가 modelcontextprotocol/servers 모노레포에 존재함이 확인되었다"
    - "4개 플러그인의 install 명령어가 공식 claude mcp add 형식 및 @modelcontextprotocol/server-* 패키지명과 일치한다"
    - "4개 플러그인의 features, keywords가 각 서버의 실제 제공 기능을 정확히 반영한다"
    - "postgres의 requiredSecrets(Database connection string)가 정확히 기재되어 있다"
    - "filesystem의 prerequisites(접근 허용 디렉토리 경로)가 정확히 기재되어 있다"
  artifacts:
    - path: "lib/plugins.ts"
      provides: "Verified metadata for filesystem, git, postgres, memory"
      contains: "verificationStatus.*verified"
    - path: "lib/i18n/plugins-en.ts"
      provides: "Synced English translations for filesystem, git, postgres, memory"
  key_links:
    - from: "lib/plugins.ts"
      to: "lib/i18n/plugins-en.ts"
      via: "plugin IDs must match between CORE_PLUGINS/PLUGIN_FIELD_OVERRIDES and pluginDescEn/reasonsEn"
      pattern: "filesystem|git|postgres|memory"
---

<objective>
Verify and update metadata for filesystem, git, postgres, and memory plugins against the modelcontextprotocol/servers monorepo READMEs.

Purpose: Ensure these 4 MCP monorepo plugins have accurate descriptions, features, install commands, keywords, requiredSecrets, and prerequisites based on their official source.
Output: Updated lib/plugins.ts and lib/i18n/plugins-en.ts with verified metadata, verificationStatus set to "verified" for confirmed plugins.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-official-mcp-monorepo-plugins/02-01-SUMMARY.md
@lib/plugins.ts
@lib/i18n/plugins-en.ts
</context>

<interfaces>
<!-- Key types and patterns from Phase 1 and Plan 01 -->

From lib/plugins.ts — same update pattern as 02-01:
- CORE_PLUGINS: update desc, longDesc, features, keywords, install, url, conflicts
- PLUGIN_FIELD_OVERRIDES: update verificationStatus, officialStatus, difficulty, requiredSecrets, bestFor, avoidFor, installMode, prerequisites

Plugin subdirectory URLs in monorepo:
- filesystem: https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem
- git: https://github.com/modelcontextprotocol/servers/tree/main/src/git
- postgres: https://github.com/modelcontextprotocol/servers/tree/main/src/postgres
- memory: https://github.com/modelcontextprotocol/servers/tree/main/src/memory

Current special fields to verify:
- postgres: difficulty "advanced", installMode "manual-required", prerequisites ["접속할 PostgreSQL 연결 문자열"], requiredSecrets ["Database connection string"]
- filesystem: difficulty "advanced", installMode "manual-required", prerequisites ["접근 허용 디렉토리 경로"]
- memory: difficulty "intermediate"
- git: no special overrides beyond defaults
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch READMEs and verify metadata for filesystem, git, postgres, memory</name>
  <files>lib/plugins.ts, lib/i18n/plugins-en.ts</files>
  <action>
For each of the 4 plugins (filesystem, git, postgres, memory), perform the following verification steps against the modelcontextprotocol/servers monorepo:

**Step 0: Fetch source data**
Use GitHub API or raw content fetch to retrieve the README.md and package.json from each plugin's subdirectory:
- `src/filesystem/`
- `src/git/`
- `src/postgres/`
- `src/memory/`

**Step 1: VERIFY-01 — Confirm subdirectory existence**
Verify each plugin's subdirectory exists in the monorepo. Log the confirmation.

**Step 2: VERIFY-02 — Description accuracy**
Compare current `desc` and `longDesc` (Korean) against the README content. Update if the README reveals features, capabilities, or use cases not reflected in current descriptions.

**Step 3: VERIFY-03 — Features accuracy**
Compare current `features` array against the actual tools/capabilities listed in the README (e.g., the "Tools" or "Features" section). Each feature should map to a real documented tool or capability.

Special attention:
- filesystem: verify sandbox/allowed-directories model, check if it provides tools like read_file, write_file, list_directory, search_files, etc.
- git: verify tools like git_log, git_diff, git_commit, git_status, etc.
- postgres: verify read-only default mode, schema inspection, query execution tools
- memory: verify knowledge graph model, entity/relation operations

**Step 4: VERIFY-04 — Keywords accuracy**
Check if `keywords` reflect actual use cases from the README. Add missing relevant keywords, remove misleading ones.

**Step 5: VERIFY-05 — Install command accuracy**
Verify install commands:
- filesystem: `claude mcp add filesystem -- npx -y @modelcontextprotocol/server-filesystem /path/to/allowed/dir` — confirm the path argument format from README
- git: `claude mcp add git -- npx -y @modelcontextprotocol/server-git` — verify package name
- postgres: `claude mcp add postgres -- npx -y @modelcontextprotocol/server-postgres postgresql://localhost:5432/mydb` — verify connection string format from README
- memory: `claude mcp add memory -- npx -y @modelcontextprotocol/server-memory` — verify package name

**Step 6: VERIFY-06 — Conflicts accuracy**
All 4 currently have `conflicts: []`. Confirm none have logical conflicts with other plugins in the DB.

**Step 7: Update PLUGIN_FIELD_OVERRIDES**
- Set `verificationStatus: "verified"` for each confirmed plugin
- Set `officialStatus: "official"` for all 4 (official MCP monorepo)
- Verify postgres `requiredSecrets`, `prerequisites`, `installMode`, `difficulty` match README
- Verify filesystem `prerequisites`, `installMode`, `difficulty` match README
- Verify memory `difficulty` and add `bestFor`/`avoidFor` if appropriate
- Add git to PLUGIN_FIELD_OVERRIDES with `verificationStatus: "verified"` and `officialStatus: "official"`

**Step 8: Sync English translations**
Update `pluginDescEn` entries for all 4 plugins to match updated Korean descriptions.
Update `reasonsEn` entries if the recommendation reasoning changed.

**What to avoid and WHY:**
- Do NOT change plugin IDs
- Do NOT modify plugins already verified in Plan 01 (sequential-thinking, brave-search, puppeteer)
- Do NOT use `any` type
- If a README is unavailable, set verificationStatus to "unverified" with a code comment
  </action>
  <verify>
    <automated>cd "E:/프로젝트/plugin-advisor" && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
- filesystem, git, postgres, memory metadata verified against monorepo READMEs
- verificationStatus updated to "verified" (or "unverified" with documented reason)
- officialStatus set to "official" for all 4
- postgres requiredSecrets and prerequisites verified
- filesystem prerequisites verified
- English translations synced in plugins-en.ts
- pnpm typecheck, lint, build all pass
  </done>
</task>

</tasks>

<verification>
- All 4 plugins have verificationStatus updated in PLUGIN_FIELD_OVERRIDES
- Install commands follow correct format with appropriate arguments (path for filesystem, connection string for postgres)
- postgres has correct requiredSecrets and prerequisites
- filesystem has correct prerequisites
- No TypeScript errors, lint errors, or build failures
- English translations match updated Korean descriptions
</verification>

<success_criteria>
- filesystem, git, postgres, memory metadata matches their README in modelcontextprotocol/servers
- Install commands use correct @modelcontextprotocol/server-* package names with correct arguments
- requiredSecrets and prerequisites correctly documented
- verificationStatus updated to "verified" for all 4 (or "unverified" with documented reason)
- pnpm typecheck && pnpm lint && pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-official-mcp-monorepo-plugins/02-02-SUMMARY.md`
</output>
